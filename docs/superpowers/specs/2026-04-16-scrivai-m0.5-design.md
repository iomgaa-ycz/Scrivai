# M0.5 PES 核心层 — 执行设计 Spec

**日期**: 2026-04-16
**里程碑**: M0.5（PES 核心层）
**范围**: T0.6 BasePES + T0.9 MockPES + T0.8 TrajectoryRecorderHook
**估时**: ~6.3d
**前置**: M0 + M0.25 已合并到 main

---

## 1. 目标

在 M0.25 基础设施层（WorkspaceManager / HookManager / TrajectoryStore）之上，构建 Scrivai 的核心执行引擎：

- **BasePES**：三阶段（plan → execute → summarize）执行抽象类，含 4 个扩展点、phase 级重试、9 hook 严格顺序、四路径 persist
- **MockPES**：按预录 `PhaseOutcome` 回放的测试替身，不依赖 `claude_agent_sdk`
- **TrajectoryRecorderHook** + **PhaseLogHook**：订阅 9 个 hook 触点，自动落盘轨迹与日志

---

## 2. 架构决策

### 2.1 决策表

| # | 决策 | 选项 | 理由 |
|---|---|---|---|
| D1 | HookManager 保持 sync | sync dispatch（M0.25 不改） | TrajectoryStore revert 为 sync 后无需 async hook |
| D2 | TrajectoryStore revert 为 sync | 去掉 `async def` + `asyncio.to_thread` | 底层是同步 sqlite，async 包装无实际收益，且级联迫使 HookManager async 化——复杂度不值得 |
| D3 | `_call_sdk_query` 默认 NotImplementedError | M0.5 不安装 claude_agent_sdk；M1 顶层 import 真实 SDK | 不使用 lazy import；MockPES override 此方法回放 turns |
| D4 | MockPES 用 PhaseOutcome dict | `dict[str, list[PhaseOutcome]]` 按 phase name + attempt_no 索引 | 声明式 + 覆盖 22 个测试场景足够 |
| D5 | BasePES 单文件 | `scrivai/pes/base.py` ~400 行 | 与 Herald2 一致，design.md §5.1 直接对照 |
| D6 | 字段名以 M0 pydantic 为准 | `phase_result` / `final_output`（非 design.md 伪代码的 `result` / `output`） | M0 模型已冻结，实现适配 |

### 2.2 与 Herald2 的对齐

| 维度 | Herald2 做法 | Scrivai M0.5 做法 |
|---|---|---|
| BasePES 文件 | `core/pes/base.py` ~585 行 | `scrivai/pes/base.py` ~400 行 |
| SDK 调用 | `call_phase_model()` 子类可 override | `_call_sdk_query()` 子类 override |
| 三阶段入口 | `plan()` / `execute_phase()` / `summarize()` | 同 |
| 重试 | LLM 调用级 | phase 级（Scrivai 契约 #6/#19 要求） |
| 扩展点 | 1 个 abstract `handle_phase_response` | 4 个默认实现（不 abstract），子类按需 override |
| Hook 系统 | pluggy sync dispatch | 同（M0.25 不改） |
| DB 持久化 | sync 调用 within async methods | 同 |
| MockPES | override `handle_phase_response` + DummyLLM | override `_call_sdk_query` + PhaseOutcome |

### 2.3 与 Herald2 的必要差异（Scrivai 契约要求）

- **phase 级重试 + cleanup**：design.md 不变量 #6/#19，attempt_no + 重试前清场
- **9 hook 严格顺序 + `on_output_written`**：不变量 #16/#17
- **`_persist_final_state` 四路径**：不变量 #20
- **hook_error 映射**：不变量 #21，同步 hook 异常 → `error_type="hook_error"` 不可重试

---

## 3. 模块设计

### 3.1 BasePES（`scrivai/pes/base.py`）

```python
class BasePES:
    def __init__(
        self,
        *,
        config: PESConfig,
        model: ModelConfig,
        workspace: WorkspaceHandle,
        hooks: HookManager | None = None,
        trajectory_store: TrajectoryStore | None = None,
        runtime_context: dict[str, Any] | None = None,
    ): ...

    # ── 公共 API ──
    async def run(self, task_prompt: str) -> PESRun

    # ── 三阶段入口 ──
    async def plan(self, run: PESRun, task_prompt: str) -> PhaseResult
    async def execute_phase(self, run: PESRun, task_prompt: str) -> PhaseResult
    async def summarize(self, run: PESRun, task_prompt: str) -> PhaseResult

    # ── 4 个扩展点（全有默认实现，子类按需 override） ──
    async def build_execution_context(self, phase: str, run: PESRun) -> dict[str, Any]
    async def build_phase_prompt(self, phase: str, phase_cfg: PhaseConfig,
                                  context: dict[str, Any], task_prompt: str) -> str
    async def postprocess_phase_result(self, phase: str, result: PhaseResult,
                                        run: PESRun) -> None
    async def validate_phase_outputs(self, phase: str, phase_cfg: PhaseConfig,
                                      result: PhaseResult, run: PESRun) -> None

    # ── SDK 调用（M0.5: NotImplementedError；M1: 真实 SDK） ──
    async def _call_sdk_query(self, phase_cfg, prompt, run, attempt_no, on_turn)
        -> tuple[str, dict, list[PhaseTurn]]

    # ── 内部机制 ──
    async def _run_phase_with_retry(self, phase, run, task_prompt) -> PhaseResult
    async def _run_phase(self, phase, run, task_prompt, attempt_no) -> PhaseResult
    def _cleanup_phase_outputs(self, phase, phase_cfg) -> None
    def _merge_context(self, runtime, execution, framework) -> dict
    def _read_previous_phase_output(self, phase) -> Any
    def _list_produced_files(self, phase) -> list[str]
    def _persist_final_state(self, run) -> None
    def _hook_error(self, phase, attempt_no, msg) -> PhaseError
    def _workspace_payload(self) -> dict[str, str]
```

#### 3.1.1 `run()` 流程

```python
async def run(self, task_prompt: str) -> PESRun:
    run = PESRun(
        run_id=self.workspace.run_id, pes_name=self.config.name,
        status="running", task_prompt=task_prompt, started_at=utcnow(),
        model_name=self.model.model, provider=self.model.provider or "",
        sdk_version="",
        skills_git_hash=self.workspace.snapshot.skills_git_hash,
        agents_git_hash=self.workspace.snapshot.agents_git_hash,
    )

    # before_run（同步 hook；异常 → run failed，不 raise）
    try:
        self.hooks.dispatch("before_run", RunHookContext(run=run))
    except Exception as e:
        run.status, run.error, run.error_type = "failed", str(e), "hook_error"
        run.ended_at = utcnow()
        self._persist_final_state(run)
        self.hooks.dispatch_non_blocking("after_run", RunHookContext(run=run))
        return run

    # 三阶段循环
    try:
        for phase in ("plan", "execute", "summarize"):
            result = await self._run_phase_with_retry(phase, run, task_prompt)
            run.phase_results[phase] = result
        run.status = "completed"
    except PhaseError as e:
        run.status, run.error = "failed", str(e)
        run.error_type = e.result.error_type if e.result else "sdk_other"
    except (KeyboardInterrupt, asyncio.CancelledError) as e:
        run.status, run.error, run.error_type = "cancelled", "interrupted", "cancelled"
        self.hooks.dispatch_non_blocking(
            "on_run_cancelled", CancelHookContext(run=run, reason=type(e).__name__))
        run.ended_at = utcnow()
        self._persist_final_state(run)
        self.hooks.dispatch_non_blocking("after_run", RunHookContext(run=run))
        raise
    finally:
        if run.status != "cancelled":
            run.ended_at = run.ended_at or utcnow()
            self._persist_final_state(run)
            self.hooks.dispatch_non_blocking("after_run", RunHookContext(run=run))

    return run
```

**调用约定**:
- 正常完成 → 返回 `PESRun(status="completed")`
- Phase 失败 / before_run hook 失败 → 返回 `PESRun(status="failed")`，不 raise
- Cancel → persist + after_run 后 **re-raise**
- 四路径都保证 `_persist_final_state` 被调一次

#### 3.1.2 `_run_phase_with_retry()`

```python
async def _run_phase_with_retry(self, phase, run, task_prompt) -> PhaseResult:
    phase_cfg = self.config.phases[phase]
    last_result = None
    for attempt_no in range(phase_cfg.max_retries + 1):
        if attempt_no > 0:
            self._cleanup_phase_outputs(phase, phase_cfg)
        try:
            return await self._run_phase(phase, run, task_prompt, attempt_no)
        except PhaseError as e:
            last_result = e.result
            will_retry = (attempt_no < phase_cfg.max_retries and e.result.is_retryable)
            self.hooks.dispatch_non_blocking(
                "on_phase_failed",
                FailureHookContext(
                    run=run, phase=phase, attempt_no=attempt_no,
                    will_retry=will_retry, error_type=e.result.error_type,
                    phase_result=e.result))
            if not will_retry:
                raise
    raise PhaseError(phase, "exhausted retries", result=last_result)
```

#### 3.1.3 `_run_phase()` — 单次尝试 9 步

```python
async def _run_phase(self, phase, run, task_prompt, attempt_no) -> PhaseResult:
    phase_cfg = self.config.phases[phase]

    # 1. before_phase hook
    try:
        self.hooks.dispatch("before_phase",
            PhaseHookContext(phase=phase, run=run, attempt_no=attempt_no))
    except Exception as e:
        raise self._hook_error(phase, attempt_no, str(e)) from e

    # 2. build_execution_context（扩展点 1）
    execution_context = await self.build_execution_context(phase, run)

    # 3. 合并双层 context
    context = self._merge_context(
        runtime=self.runtime_context or {},
        execution=execution_context,
        framework={"phase": phase, "attempt_no": attempt_no,
                    "workspace": self._workspace_payload(),
                    "previous_phase_output": self._read_previous_phase_output(phase)})

    # 4. build_phase_prompt（扩展点 2）+ before_prompt hook
    prompt = await self.build_phase_prompt(phase, phase_cfg, context, task_prompt)
    prompt_ctx = PromptHookContext(phase=phase, run=run, attempt_no=attempt_no, prompt=prompt)
    try:
        self.hooks.dispatch("before_prompt", prompt_ctx)
    except Exception as e:
        raise self._hook_error(phase, attempt_no, str(e)) from e
    prompt = prompt_ctx.prompt

    # 5. _call_sdk_query（子类 override 点）
    started_at = utcnow()
    response_text, usage, turns = await self._call_sdk_query(
        phase_cfg, prompt, run, attempt_no,
        on_turn=lambda t: self.hooks.dispatch(
            "after_prompt_turn",
            PromptTurnHookContext(phase=phase, run=run, attempt_no=attempt_no, turn=t)))

    # 6. 构造 PhaseResult
    result = PhaseResult(
        phase=phase, attempt_no=attempt_no, prompt=prompt,
        response_text=response_text, turns=turns, usage=usage,
        produced_files=self._list_produced_files(phase),
        started_at=started_at, ended_at=utcnow())

    # 7. postprocess_phase_result（扩展点 3）
    try:
        await self.postprocess_phase_result(phase, result, run)
    except Exception as e:
        result.error, result.error_type, result.is_retryable = str(e), "response_parse_error", False
        raise PhaseError(phase, str(e), result=result) from e

    # 8. validate_phase_outputs（扩展点 4）
    try:
        await self.validate_phase_outputs(phase, phase_cfg, result, run)
    except Exception as e:
        result.error, result.error_type, result.is_retryable = str(e), "output_validation_error", True
        raise PhaseError(phase, str(e), result=result) from e

    # 9a. on_output_written（仅 summarize + validate 通过后）
    if phase == "summarize":
        try:
            self.hooks.dispatch("on_output_written",
                OutputHookContext(run=run,
                                  output_path=self.workspace.working_dir / "output.json",
                                  final_output=run.final_output or {}))
        except Exception as e:
            raise self._hook_error(phase, attempt_no, str(e)) from e

    # 9b. after_phase hook
    try:
        self.hooks.dispatch("after_phase",
            PhaseHookContext(phase=phase, run=run, attempt_no=attempt_no, phase_result=result))
    except Exception as e:
        raise self._hook_error(phase, attempt_no, str(e)) from e

    return result
```

#### 3.1.4 辅助方法

```python
def _hook_error(self, phase, attempt_no, msg) -> PhaseError:
    result = PhaseResult(phase=phase, attempt_no=attempt_no, prompt="",
                         response_text="", turns=[], usage={}, produced_files=[],
                         started_at=utcnow(), ended_at=utcnow(),
                         error=msg, error_type="hook_error", is_retryable=False)
    return PhaseError(phase, msg, result=result)

def _persist_final_state(self, run: PESRun) -> None:
    if self.trajectory_store is None:
        return
    self.trajectory_store.finalize_run(
        run_id=run.run_id, status=run.status,
        final_output=run.final_output,
        workspace_archive_path=None,
        error=run.error, error_type=run.error_type)

def _cleanup_phase_outputs(self, phase: str, phase_cfg: PhaseConfig) -> None:
    working = self.workspace.working_dir
    for rule in phase_cfg.required_outputs:
        if isinstance(rule, str):
            (working / rule).unlink(missing_ok=True)
        elif isinstance(rule, dict) and "path" in rule:
            target = working / rule["path"]
            if target.is_dir():
                shutil.rmtree(target, ignore_errors=False)
            elif target.exists():
                target.unlink()

def _merge_context(self, runtime, execution, framework) -> dict[str, Any]:
    merged = {}
    merged.update(runtime)
    merged.update(execution)
    merged.update(framework)
    return merged

def _read_previous_phase_output(self, phase: str) -> Any:
    working = self.workspace.working_dir
    if phase == "execute":
        plan_json = working / "plan.json"
        if plan_json.exists():
            return json.loads(plan_json.read_text(encoding="utf-8"))
    elif phase == "summarize":
        findings = working / "findings"
        if findings.is_dir():
            return {f.name: json.loads(f.read_text(encoding="utf-8"))
                    for f in sorted(findings.glob("*.json"))}
    return None

def _list_produced_files(self, phase: str) -> list[str]:
    working = self.workspace.working_dir
    all_files = []
    for p in working.rglob("*"):
        if p.is_file() and not p.name.startswith("."):
            all_files.append(str(p.relative_to(working)))
    return sorted(all_files)

def _workspace_payload(self) -> dict[str, str]:
    return {"working_dir": str(self.workspace.working_dir),
            "data_dir": str(self.workspace.data_dir),
            "output_dir": str(self.workspace.output_dir)}
```

#### 3.1.5 默认扩展点实现

```python
async def build_execution_context(self, phase: str, run: PESRun) -> dict[str, Any]:
    return {}

async def build_phase_prompt(self, phase: str, phase_cfg: PhaseConfig,
                              context: dict[str, Any], task_prompt: str) -> str:
    parts = []
    if self.config.prompt_text:
        parts.append(self.config.prompt_text)
    if phase_cfg.additional_system_prompt:
        parts.append(phase_cfg.additional_system_prompt)
    parts.append(task_prompt)
    if context:
        parts.append(json.dumps(context, ensure_ascii=False, default=str))
    return "\n\n".join(parts)

async def postprocess_phase_result(self, phase: str, result: PhaseResult,
                                    run: PESRun) -> None:
    return None

async def validate_phase_outputs(self, phase: str, phase_cfg: PhaseConfig,
                                  result: PhaseResult, run: PESRun) -> None:
    working = self.workspace.working_dir
    for rule in phase_cfg.required_outputs:
        if isinstance(rule, str):
            if not (working / rule).exists():
                raise PhaseError(phase, f"required output missing: {rule}")
        elif isinstance(rule, dict):
            path = working / rule["path"]
            if not path.is_dir():
                raise PhaseError(phase, f"required directory missing: {rule['path']}")
            pattern = rule.get("pattern", "*")
            min_files = rule.get("min_files", 1)
            found = list(path.glob(pattern))
            if len(found) < min_files:
                raise PhaseError(phase,
                    f"{rule['path']}: expected >= {min_files} files matching '{pattern}', found {len(found)}")
```

#### 3.1.6 `_call_sdk_query` 默认实现

```python
async def _call_sdk_query(
    self,
    phase_cfg: PhaseConfig,
    prompt: str,
    run: PESRun,
    attempt_no: int,
    on_turn: Callable[[PhaseTurn], None],
) -> tuple[str, dict[str, Any], list[PhaseTurn]]:
    raise NotImplementedError(
        "M1 安装 claude-agent-sdk 后实现；"
        "测试请使用 MockPES 或子类 override 此方法。"
    )
```

---

### 3.2 MockPES（`scrivai/testing/mock_pes.py`）

```python
@dataclass
class PhaseOutcome:
    """一次 phase 尝试的预录结果。"""
    response_text: str = ""
    turns: list[PhaseTurn] = field(default_factory=list)
    usage: dict[str, Any] = field(default_factory=dict)
    error: str | None = None
    error_type: PhaseErrorType | None = None
    is_retryable: bool = False
    produced_files: list[str] = field(default_factory=list)


class MockPES(BasePES):
    """按预录 PhaseOutcome 回放，不依赖 claude_agent_sdk。"""

    def __init__(
        self,
        *,
        config: PESConfig,
        workspace: WorkspaceHandle,
        hooks: HookManager | None = None,
        trajectory_store: TrajectoryStore | None = None,
        runtime_context: dict[str, Any] | None = None,
        phase_outcomes: dict[str, list[PhaseOutcome]] | None = None,
    ):
        super().__init__(
            config=config, model=ModelConfig(model="mock-model"),
            workspace=workspace, hooks=hooks,
            trajectory_store=trajectory_store, runtime_context=runtime_context)
        self._outcomes = phase_outcomes or {}

    async def _call_sdk_query(self, phase_cfg, prompt, run, attempt_no, on_turn):
        outcomes = self._outcomes.get(phase_cfg.name, [])
        if attempt_no >= len(outcomes):
            return "", {}, []

        outcome = outcomes[attempt_no]
        for turn in outcome.turns:
            on_turn(turn)

        if outcome.error:
            result = PhaseResult(
                phase=phase_cfg.name, attempt_no=attempt_no,
                prompt=prompt, response_text=outcome.response_text,
                turns=outcome.turns, usage=outcome.usage,
                produced_files=outcome.produced_files,
                started_at=utcnow(), ended_at=utcnow(),
                error=outcome.error, error_type=outcome.error_type,
                is_retryable=outcome.is_retryable)
            raise PhaseError(phase_cfg.name, outcome.error, result=result)

        # 模拟 Agent 产出文件
        for f in outcome.produced_files:
            path = self.workspace.working_dir / f
            path.parent.mkdir(parents=True, exist_ok=True)
            if not path.exists():
                path.write_text("{}", encoding="utf-8")

        return outcome.response_text, outcome.usage, outcome.turns
```

---

### 3.3 TrajectoryRecorderHook（`scrivai/trajectory/hooks.py`）

```python
class TrajectoryRecorderHook:
    """订阅全部 9 个 hook，sync 调 TrajectoryStore 落盘。"""

    def __init__(self, store: TrajectoryStore) -> None:
        self._store = store
        self._phase_ids: dict[str, int] = {}   # "run_id:phase:attempt" → phase_id

    @hookimpl
    def before_run(self, context: RunHookContext) -> None:
        run = context.run
        self._store.start_run(
            run_id=run.run_id, pes_name=run.pes_name,
            model_name=run.model_name, provider=run.provider,
            sdk_version=run.sdk_version,
            skills_git_hash=run.skills_git_hash, agents_git_hash=run.agents_git_hash,
            skills_is_dirty=run.skills_is_dirty,
            task_prompt=run.task_prompt, runtime_context=None)

    @hookimpl
    def before_phase(self, context: PhaseHookContext) -> None:
        key = f"{context.run.run_id}:{context.phase}:{context.attempt_no}"
        phase_id = self._store.record_phase_start(
            run_id=context.run.run_id, phase_name=context.phase,
            phase_order=["plan", "execute", "summarize"].index(context.phase),
            attempt_no=context.attempt_no)
        self._phase_ids[key] = phase_id

    @hookimpl
    def after_prompt_turn(self, context: PromptTurnHookContext) -> None:
        key = f"{context.run.run_id}:{context.phase}:{context.attempt_no}"
        phase_id = self._phase_ids.get(key)
        if phase_id is None:
            return
        turn = context.turn
        turn_id = self._store.record_turn(
            phase_id=phase_id, turn_index=turn.turn_index,
            role=turn.role, content_type=turn.content_type, data=turn.data)
        if turn.content_type == "tool_use":
            self._store.record_tool_call(
                turn_id=turn_id, tool_name=turn.data.get("name", ""),
                tool_input=turn.data.get("input"), tool_output=None,
                status="started", duration_ms=None)
        elif turn.content_type == "tool_result":
            self._store.record_tool_call(
                turn_id=turn_id, tool_name=turn.data.get("name", ""),
                tool_input=None, tool_output=turn.data.get("content"),
                status="completed", duration_ms=None)

    @hookimpl
    def after_phase(self, context: PhaseHookContext) -> None:
        self._record_phase_end(context.run.run_id, context.phase,
                                context.attempt_no, context.phase_result)

    @hookimpl
    def on_phase_failed(self, context: FailureHookContext) -> None:
        self._record_phase_end(context.run.run_id, context.phase,
                                context.attempt_no, context.phase_result)

    @hookimpl
    def on_output_written(self, context: OutputHookContext) -> None:
        pass

    @hookimpl
    def on_run_cancelled(self, context: CancelHookContext) -> None:
        pass

    @hookimpl
    def after_run(self, context: RunHookContext) -> None:
        pass

    def _record_phase_end(self, run_id, phase, attempt_no, result) -> None:
        key = f"{run_id}:{phase}:{attempt_no}"
        phase_id = self._phase_ids.get(key)
        if phase_id is None or result is None:
            return
        self._store.record_phase_end(
            phase_id=phase_id, prompt=result.prompt,
            response_text=result.response_text, produced_files=result.produced_files,
            usage=result.usage, error=result.error,
            error_type=result.error_type, is_retryable=result.is_retryable)
```

### 3.4 PhaseLogHook（`scrivai/pes/phase_log.py`）

```python
class PhaseLogHook:
    """把每次 phase 尝试的 prompt/response/turns dump 到 logs_dir。"""

    def __init__(self, workspace: WorkspaceHandle) -> None:
        self._logs_dir = workspace.logs_dir

    @hookimpl
    def after_phase(self, context: PhaseHookContext) -> None:
        self._dump(context.phase, context.attempt_no, context.phase_result)

    @hookimpl
    def on_phase_failed(self, context: FailureHookContext) -> None:
        self._dump(context.phase, context.attempt_no, context.phase_result)

    def _dump(self, phase, attempt_no, result) -> None:
        if result is None:
            return
        log_path = self._logs_dir / f"phase-{phase}-attempt-{attempt_no}.log.json"
        log_path.write_text(
            json.dumps({
                "phase": phase, "attempt_no": attempt_no,
                "prompt": result.prompt, "response_text": result.response_text,
                "turns": [t.model_dump(mode="json") for t in result.turns],
                "usage": result.usage, "error": result.error,
            }, ensure_ascii=False, indent=2),
            encoding="utf-8")
```

---

## 4. 补充改动（随 T0.6 commit 一起做）

| 改动 | 文件 | 说明 |
|---|---|---|
| `PhaseError` 增加 `result` 属性 | `scrivai/exceptions.py` | `__init__(self, phase, msg, result=None)`；加 `self.phase` / `self.result` |
| `PESRun.sdk_version` 改默认空串 | `scrivai/models/pes.py` | `sdk_version: str = ""`；MockPES 无需显式传 |
| `PESRun.provider` 改默认空串 | `scrivai/models/pes.py` | `provider: str = ""`；同理 |
| `PESRun.to_prompt_payload()` | `scrivai/models/pes.py` | 返回 `{"run_id", "pes_name", "status", "phase_results"}` 简单 dict |
| TrajectoryStore revert 为 sync | `scrivai/trajectory/store.py` | 去掉 `async def` + `await asyncio.to_thread()`；9 个测试机械改 |
| `__init__.py` 增量导出 | `scrivai/__init__.py` | 加 `BasePES, MockPES, TrajectoryRecorderHook, PhaseLogHook, PhaseOutcome` |

---

## 5. 契约测试矩阵

### 5.1 `tests/contract/test_base_pes.py`（16 个）

| 测试 | 覆盖 |
|---|---|
| `test_three_phase_order` | 不变量 #4 |
| `test_plan_failure_skips_execute_and_summarize` | 不变量 #4 |
| `test_hooks_called_in_correct_order_nine_hooks` | 不变量 #16 |
| `test_context_layering` | §4.2 双层 context 合并 |
| `test_required_outputs_enforced` | §4.2 结构化规则 `{"path","min_files","pattern"}` |
| `test_phase_retry_on_validation_failure` | 不变量 #6 |
| `test_phase_no_retry_on_parse_error` | 不变量 #5 |
| `test_cleanup_before_retry` | 不变量 #19 |
| `test_cancellation_dispatches_on_run_cancelled` | 不变量 #18 |
| `test_on_output_written_only_on_summarize` | 不变量 #17 |
| `test_extension_points_selective_override` | §4.2 子类只覆盖部分扩展点 |
| `test_hook_error_maps_to_phase_failure` | 不变量 #21 |
| `test_before_run_hook_error_fails_run` | 不变量 #21 |
| `test_nonblocking_hook_error_not_propagated` | 不变量 #21 |
| `test_finalize_run_called_on_all_paths` | 不变量 #20 |
| `test_after_phase_hook_error_on_summarize_marks_run_failed` | 不变量 #21 |

### 5.2 `tests/contract/test_mock_pes.py`（4 个）

| 测试 | 覆盖 |
|---|---|
| `test_happy_path_three_phases` | MockPES 三阶段正常回放 |
| `test_injected_failure_triggers_retry` | PhaseOutcome error 注入 + 重试 |
| `test_default_outcome_when_not_specified` | 空 outcomes 默认 happy path |
| `test_produced_files_created_in_workspace` | 文件自动创建到 workspace |

### 5.3 `tests/contract/test_trajectory_hook.py`（2 个）

| 测试 | 覆盖 |
|---|---|
| `test_full_run_recorded` | 不变量 #9：runs/phases/turns 全表有对应记录 |
| `test_tool_calls_extracted_from_turns` | 不变量 #9：tool_use/tool_result 正确拆解到 tool_calls 表 |

**共 22 个契约测试。**

---

## 6. Commit 计划

| 序号 | Commit message | 内容 |
|---|---|---|
| 1 | `feat: add BasePES abstract engine (T0.6)` | `base.py` + `phase_log.py` + `test_base_pes.py` + `exceptions.py` 补 PhaseError.result + `models/pes.py` 补 to_prompt_payload + TrajectoryStore revert sync |
| 2 | `feat: add MockPES test double (T0.9)` | `testing/mock_pes.py` + `test_mock_pes.py` |
| 3 | `feat: add TrajectoryRecorderHook (T0.8)` | `trajectory/hooks.py` + `test_trajectory_hook.py` + `__init__.py` 增量符号 |

---

## 7. 风险

| 风险 | 缓解 |
|---|---|
| TrajectoryStore sync revert 可能影响 M0.25 测试 | 机械替换 `await` → 直接调用；先跑全量测试验证 |
| `_run_phase` 9 步流程复杂度高 | 逐步 TDD：先写测试再实现，每步一个 try/except |
| MockPES PhaseOutcome 不够灵活 | 22 个测试验证覆盖度；不足时加字段，不改架构 |
| PhaseError.result 属性新加可能与 M0 PhaseError 签名不兼容 | 只加 `__init__` override，保持 `ScrivaiError` 继承链 |

---

## 8. Scope Guard

**不在 M0.5**:
- `_call_sdk_query` 真实 SDK 实现（M1）
- 三个预置 PES（M1）
- L1 传输级重试 RateLimitError 指数退避（M1）
- HookManager 改动（保持 M0.25 sync 不变）
- `record_feedback` / `get_feedback_pairs` 验证（M2）
