# Scrivai M0 地基层 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 Scrivai v3 地基层(legacy 清理 + 全部 pydantic/Protocol 模型 + PESConfig YAML loader + Evolution 占位),交付 4 个契约测试集全绿、`mypy --strict scrivai/models/` 通过的 M0 状态。

**Architecture:** 在 `feat/scrivai-m0-foundation` 分支上分 3 个粗粒度 commit:(1) 提交 v3 spec 文档定稿;(2) 删除 legacy v2 代码、改写 conftest、更新 pyproject 依赖;(3) 实现 `scrivai/exceptions.py` + `scrivai/models/` + `scrivai/pes/config.py` + `scrivai/evolution/` 占位 + `tests/contract/` 4 个测试集。

**Tech Stack:** Python 3.11 / pydantic v2.6+ / pyyaml 6.0+ / qmd 0.1.2 / pytest 8.0+ / mypy 1.10+(strict + pydantic plugin) / ruff 0.4+

**Spec 参考**: `docs/superpowers/specs/2026-04-16-scrivai-m0-design.md`(已 commit `519efdc`)
**契约权威**: `docs/design.md` §4.1 / §4.5 / §4.6
**任务上下文**: `docs/TD.md` §M0(T0.2 / T0.3 / T0.18)

**spec 与现实修正**(plan 内执行时遵循):
1. spec §3.2.2 `tests/e2e/test_doc_pipeline_e2e.py` → 实际在 `tests/integration/test_doc_pipeline_e2e.py`(`tests/e2e/` 仅有空 `__init__.py`)
2. spec §3.2.4 `qmd>=0.1.0` → 升级到 `qmd>=0.1.2`(0.1.0 不导出 ChunkRef/CollectionInfo/Collection/QmdClient/connect)

---

## File Structure

### 新建文件(M0 末态)

```
scrivai/
├── __init__.py                          [REWRITE] 顶层 Public API:qmd re-exports + EvolutionTrigger/run_evolution
├── exceptions.py                        [NEW] 5 类异常前置声明
├── models/
│   ├── __init__.py                      [NEW] 聚合所有 pydantic + Protocol
│   ├── pes.py                           [NEW] ModelConfig/PhaseConfig/PESConfig/PhaseTurn/PhaseResult/PESRun + 9 HookContext
│   ├── workspace.py                     [NEW] WorkspaceSpec/Snapshot/Handle + WorkspaceManager Protocol
│   ├── knowledge.py                     [NEW] LibraryEntry + Library Protocol + qmd re-export(ChunkRef/SearchResult/CollectionInfo)
│   ├── trajectory.py                    [NEW] TrajectoryRecord/PhaseRecord/FeedbackRecord
│   └── evolution.py                     [NEW] EvolutionConfig/Run/FeedbackExample + Evaluator/SkillsRootResolver Protocol
├── pes/
│   ├── __init__.py                      [NEW] 导出 load_pes_config
│   └── config.py                        [NEW] load_pes_config YAML loader
└── evolution/
    ├── __init__.py                      [NEW] 导出 EvolutionTrigger / run_evolution(M2 占位)
    ├── trigger.py                       [NEW] EvolutionTrigger 占位类
    ├── runner.py                        [NEW] run_evolution 占位函数
    └── evaluator.py                     [NEW] 空 docstring(M2 实现内置 Evaluator 示例)

tests/
├── conftest.py                          [REWRITE] 仅保留 load_dotenv()
└── contract/
    ├── __init__.py                      [NEW] 空
    ├── conftest.py                      [NEW] 共享 fixtures
    ├── test_models.py                   [NEW] T0.2 契约测试
    ├── test_reexport.py                 [NEW] qmd 身份相等
    ├── test_pes_config.py               [NEW] T0.3 契约
    └── test_evolution_stubs.py          [NEW] T0.18 契约

pyproject.toml                           [MODIFY] 删 litellm/jinja2,加 pydantic>=2.6/mypy>=1.10/qmd>=0.1.2,加 [tool.mypy]
```

### 被删除文件

```
scrivai/audit/, scrivai/generation/, scrivai/knowledge/, scrivai/utils/  整个目录
scrivai/llm.py, scrivai/project.py, scrivai/chunkers.py
tests/unit/test_*.py(13 个 legacy)
tests/integration/test_*.py(6 个 legacy,含 test_doc_pipeline_e2e.py)
docs/architecture.md, docs/sdk_design.md
```

---

## Commit 1: `docs: finalize v3 spec`

### Task 1: 提交 v3 spec 定稿文档

**Files:**
- Modify: `CLAUDE.md` (working tree 已改)
- Modify: `README.md` (working tree 已改)
- Modify: `docs/design.md` (working tree 已改)
- Delete: `docs/architecture.md` (已被 design.md 取代)
- Delete: `docs/sdk_design.md` (已被 design.md 取代)

- [ ] **Step 1: 确认工作树状态**

Run: `git status --short`
Expected:
```
 M CLAUDE.md
 M README.md
 D docs/architecture.md
 M docs/design.md
 D docs/sdk_design.md
```
若额外文件出现,先排查;若有少文件,说明已被部分 stage,继续。

- [ ] **Step 2: 暂存全部 docs 改动**

Run: `git add CLAUDE.md README.md docs/architecture.md docs/design.md docs/sdk_design.md`

- [ ] **Step 3: 验证 stage 内容**

Run: `git diff --cached --stat`
Expected: 5 个文件改动,删除 2 个、修改 3 个。

- [ ] **Step 4: 提交 Commit 1**

Run:
```bash
git commit -m "$(cat <<'EOF'
docs: finalize v3 spec

- 用 design.md 收敛原 architecture.md / sdk_design.md
- CLAUDE.md / README.md 更新到 v3 框架定位
EOF
)"
```
Expected: 一行 `[feat/scrivai-m0-foundation <hash>] docs: finalize v3 spec`,5 file changed。

- [ ] **Step 5: 验证 git log**

Run: `git log --oneline -3`
Expected: 新 commit 在最上,前一个是 spec 文档(`519efdc docs: add M0 execution design spec`)。

---

## Commit 2: `refactor: clean legacy v2 code for v3 foundation`

### Task 2: 删除 legacy 源码目录与文件

**Files:**
- Delete: `scrivai/audit/`, `scrivai/generation/`, `scrivai/knowledge/`, `scrivai/utils/` (整目录)
- Delete: `scrivai/llm.py`, `scrivai/project.py`, `scrivai/chunkers.py`

- [ ] **Step 1: 删除 4 个 legacy 子目录**

Run:
```bash
rm -rf scrivai/audit scrivai/generation scrivai/knowledge scrivai/utils
```
Expected: 无输出(成功)。

- [ ] **Step 2: 删除 3 个 legacy 顶层模块**

Run:
```bash
rm scrivai/llm.py scrivai/project.py scrivai/chunkers.py
```
Expected: 无输出。

- [ ] **Step 3: 验证 scrivai/ 目录精简**

Run: `ls scrivai/`
Expected: 仅剩 `__init__.py`(以及 `__pycache__` 自动生成,可忽略)。

### Task 3: 删除 legacy 测试文件

**Files:**
- Delete: `tests/unit/test_*.py` (13 个)
- Delete: `tests/integration/test_*.py` (6 个)
- Keep: `tests/unit/__init__.py`, `tests/integration/__init__.py`, `tests/e2e/__init__.py`(空目录占位)

- [ ] **Step 1: 删除 unit 测试**

Run:
```bash
rm tests/unit/test_audit.py tests/unit/test_audit_real.py \
   tests/unit/test_chunkers.py \
   tests/unit/test_doc_pipeline.py tests/unit/test_doc_pipeline_real.py \
   tests/unit/test_generation.py tests/unit/test_generation_real.py \
   tests/unit/test_knowledge.py \
   tests/unit/test_llm.py tests/unit/test_llm_real.py \
   tests/unit/test_office_tools.py \
   tests/unit/test_project.py tests/unit/test_project_real.py
```
Expected: 无输出。

- [ ] **Step 2: 删除 integration 测试**

Run:
```bash
rm tests/integration/test_audit_flow.py \
   tests/integration/test_doc_pipeline_e2e.py \
   tests/integration/test_generation_audit_cycle.py \
   tests/integration/test_generation_flow.py \
   tests/integration/test_multichapter_flow.py \
   tests/integration/test_project_sdk_flow.py
```
Expected: 无输出。

- [ ] **Step 3: 验证 tests/ 目录精简**

Run: `find tests -name "*.py" -not -name "__init__.py"`
Expected: 仅 `tests/conftest.py`。

### Task 4: 更新 pyproject.toml

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: 重写 pyproject.toml**

完整内容如下(覆盖现有文件):

```toml
[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scrivai"
version = "0.2.0a0"
description = "Claude Agent 编排框架 — 文档审核 / 生成场景"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
authors = [
    {name = "chengzhang yu", email = "iomgaaycz@gmail.com"}
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "pydantic>=2.6",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
    "qmd>=0.1.2",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.4",
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "mypy>=1.10",
]

[project.urls]
Homepage = "https://github.com/iomgaa/scrivai"
Repository = "https://github.com/iomgaa/scrivai"
Issues = "https://github.com/iomgaa/scrivai/issues"

[tool.setuptools.packages.find]
include = ["scrivai*"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.mypy]
plugins = ["pydantic.mypy"]
python_version = "3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
markers = [
    "contract: 契约测试(契约稳定性 / 跨项目协议)",
    "integration: 真实 API 调用测试(需要网络和 API key)",
    "unit: 单元测试(使用 mock)",
]
addopts = "-v --tb=short"
```

- [ ] **Step 2: 验证 toml 语法**

Run: `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"`
Expected: 无报错。

### Task 5: 改写 tests/conftest.py 为最小化

**Files:**
- Modify: `tests/conftest.py`

- [ ] **Step 1: 完整改写**

完整内容(覆盖现有):

```python
"""Scrivai 测试根配置——加载 .env(为 M1 API key 留接口)。"""
from dotenv import load_dotenv

load_dotenv()
```

- [ ] **Step 2: 静态语法验证**

Run: `python -c "import ast; ast.parse(open('tests/conftest.py').read())"`
Expected: 无报错。

### Task 6: 清空 scrivai/__init__.py(Commit 2 末态)

**Files:**
- Modify: `scrivai/__init__.py`

> **注**:本任务清空到仅 docstring。Task 25 在 Commit 3 内会再次改写为最终态(加 qmd + evolution 顶层 re-exports)。

- [ ] **Step 1: 完整改写**

完整内容(覆盖现有):

```python
"""Scrivai v3 — Claude Agent 编排框架(地基层 M0,完整 Public API 在 M0.75 冻结)。"""
```

### Task 7: 安装新依赖,验证 Commit 2 DoD,提交

- [ ] **Step 1: 安装更新后的依赖**

Run: `conda run -n scrivai pip install -e ".[dev]"`
Expected: 安装成功;`Successfully installed mypy-X.Y.Z pydantic-2.X.Y ...`(具体版本视环境)。

- [ ] **Step 2: legacy 符号清洁性检查**

Run:
```bash
git grep -E "LLMClient|AuditEngine|GenerationEngine|ProjectConfig|KnowledgeStore" -- scrivai/ tests/
```
Expected: 退出码 1(grep 无匹配),零输出。**若有任何输出**,说明清理不彻底,回到 Task 2/3 排查。

- [ ] **Step 3: pytest 收集验证**

Run: `conda run -n scrivai pytest tests/ -v --collect-only`
Expected: `collected 0 items`(无 import 报错)。

- [ ] **Step 4: 暂存全部 Commit 2 改动**

Run: `git status --short`
Expected:
```
 M pyproject.toml
 D scrivai/audit/...(多行)
 D scrivai/generation/...
 D scrivai/knowledge/...
 D scrivai/utils/...
 D scrivai/chunkers.py
 D scrivai/llm.py
 D scrivai/project.py
 M scrivai/__init__.py
 D tests/integration/test_audit_flow.py(多行)
 D tests/integration/test_doc_pipeline_e2e.py
 D tests/integration/test_generation_audit_cycle.py
 D tests/integration/test_generation_flow.py
 D tests/integration/test_multichapter_flow.py
 D tests/integration/test_project_sdk_flow.py
 M tests/conftest.py
 D tests/unit/test_audit.py(多行)
 D tests/unit/...(共 13 个)
```

Run: `git add -A pyproject.toml scrivai/ tests/`

- [ ] **Step 5: 提交 Commit 2**

Run:
```bash
git commit -m "$(cat <<'EOF'
refactor: clean legacy v2 code for v3 foundation

- 删除 audit/generation/knowledge/utils legacy 子包
- 删除 llm.py/project.py/chunkers.py 顶层模块
- 删除 tests/unit/test_*.py(13 个)与 tests/integration/test_*.py(6 个)
- pyproject: 删 litellm/jinja2,加 pydantic>=2.6/mypy>=1.10,升 qmd>=0.1.2,版本 0.2.0a0
- pyproject: 加 [tool.mypy] plugins = ["pydantic.mypy"]
- tests/conftest.py 重写为仅 load_dotenv()
- scrivai/__init__.py 清空为仅 docstring(Commit 3 再补 re-exports)
EOF
)"
```
Expected: 一行 `[feat/scrivai-m0-foundation <hash>] refactor: clean legacy v2 code for v3 foundation`。

- [ ] **Step 6: 二次验证**

Run: `conda run -n scrivai python -c "import scrivai; print(scrivai.__doc__)"`
Expected: 输出 docstring 内容,无 ImportError。

---

## Commit 3: `feat: add M0 models + config + evolution stubs + contract tests`

> **TDD 节奏**:本 commit 内每个文件按"先写测试 → 跑 → 失败 → 实现 → 跑 → 通过"的 cycle 实施。
> **执行顺序依赖**:exceptions.py → models/ → tests/contract/conftest.py + test_models.py + test_reexport.py → pes/config.py → test_pes_config.py → evolution/ → test_evolution_stubs.py → scrivai/__init__.py 终态 → 全量验证。

### Task 8: 创建 scrivai/exceptions.py

**Files:**
- Create: `scrivai/exceptions.py`

- [ ] **Step 1: 写 inline smoke 测试**

不为 exceptions 单独建测试文件;在后续 `test_models.py` 中包含一个 `test_exceptions_importable` 函数即可(Task 11)。本任务只需实现 + 手动验证。

- [ ] **Step 2: 实现 scrivai/exceptions.py**

```python
"""Scrivai 异常层级——M0 前置声明,后续里程碑只补行为。"""


class ScrivaiError(Exception):
    """所有 Scrivai 异常的根基类。"""


class PESConfigError(ScrivaiError):
    """PESConfig YAML 加载 / schema 校验失败。M0 T0.3 实现。"""


class WorkspaceError(ScrivaiError):
    """WorkspaceManager 错误(run_id 冲突 / fcntl 失败等)。M0.25 T0.4 实现。"""


class TrajectoryWriteError(ScrivaiError):
    """TrajectoryStore 写入失败(SQLite busy 超过重试预算)。M0.25 T0.7 实现。"""


class PhaseError(ScrivaiError):
    """BasePES phase 级失败统一出口。携带 result / error_type / is_retryable。M0.5 T0.6 实现。"""


class RateLimitError(ScrivaiError):
    """Claude SDK 速率限制;用于 L1 传输级重试。M0.5 T0.6 实现。"""
```

- [ ] **Step 3: 手动 import 验证**

Run:
```bash
conda run -n scrivai python -c "
from scrivai.exceptions import (
    ScrivaiError, PESConfigError, WorkspaceError,
    TrajectoryWriteError, PhaseError, RateLimitError,
)
assert issubclass(PESConfigError, ScrivaiError)
assert issubclass(PhaseError, ScrivaiError)
print('OK')
"
```
Expected: `OK`。

### Task 9: 创建 tests/contract/ 目录骨架与 conftest.py

**Files:**
- Create: `tests/contract/__init__.py` (空)
- Create: `tests/contract/conftest.py`

- [ ] **Step 1: 创建 tests/contract/__init__.py**

完整内容:
```python
```
(空文件)

Run: `touch tests/contract/__init__.py`(或用 Write tool 创建空文件)

- [ ] **Step 2: 实现 tests/contract/conftest.py**

完整内容:

```python
"""M0 契约测试共享 fixtures。

T0.16(M0.75)将把这里的 fixture "毕业"为 scrivai.testing.contract pytest plugin
供下游(GovDoc 等)直接复用。
"""
from __future__ import annotations

import os
from datetime import datetime, timezone
from pathlib import Path

import pytest


@pytest.fixture
def sample_pes_config_yaml(tmp_path: Path) -> Path:
    """生成一份合法的 extractor PESConfig YAML 写到 tmp,返回 Path。"""
    yaml_text = """\
name: extractor
display_name: 通用条目抽取
prompt_text: |
  You are a structured information extractor.
default_skills:
  - available-tools
phases:
  plan:
    additional_system_prompt: "draft an extraction strategy."
    allowed_tools: [Bash, Read, Write]
    max_turns: 6
    max_retries: 1
    permission_mode: default
    required_outputs: [plan.md, plan.json]
  execute:
    additional_system_prompt: "execute plan.json."
    allowed_tools: [Bash, Read, Write]
    max_turns: 30
    max_retries: 1
    permission_mode: default
    required_outputs:
      - {path: "findings/", min_files: 1, pattern: "*.json"}
  summarize:
    additional_system_prompt: "aggregate."
    allowed_tools: [Bash, Read, Write]
    max_turns: 4
    max_retries: 1
    permission_mode: default
    required_outputs: [output.json]
"""
    p = tmp_path / "extractor.yaml"
    p.write_text(yaml_text, encoding="utf-8")
    return p


@pytest.fixture
def sample_phase_result_dict() -> dict:
    """合法 PhaseResult dict,用于 model_dump/model_validate 回环。"""
    now = datetime.now(timezone.utc).isoformat()
    return {
        "phase": "plan",
        "attempt_no": 0,
        "prompt": "you are a planner.",
        "response_text": "{}",
        "turns": [],
        "produced_files": ["plan.md", "plan.json"],
        "usage": {"input_tokens": 100, "output_tokens": 200},
        "started_at": now,
        "ended_at": now,
        "error": None,
        "error_type": None,
        "is_retryable": False,
    }


@pytest.fixture
def env_var_set(monkeypatch: pytest.MonkeyPatch):
    """提供一个可设置环境变量并自动恢复的辅助。"""
    def _set(key: str, value: str) -> None:
        monkeypatch.setenv(key, value)
    return _set
```

- [ ] **Step 3: 验证 fixture 可被 pytest 发现**

Run: `conda run -n scrivai pytest tests/contract/ --collect-only -q`
Expected: `no tests ran`(目录里还没测试文件,但 conftest 不报错)。

### Task 10: 写 tests/contract/test_models.py(全部 failing)

**Files:**
- Create: `tests/contract/test_models.py`

- [ ] **Step 1: 实现完整测试文件**

完整内容(契约测试都先写好,Task 11-17 实现 models 让它们通过):

```python
"""T0.2 契约测试:全部 pydantic / Protocol 模型。

参考 docs/design.md §4.1。
"""
from __future__ import annotations

from datetime import datetime, timezone
from pathlib import Path
from typing import get_args

import pytest
from pydantic import ValidationError


# ────── 子模块导入完整性 ──────

def test_exceptions_importable() -> None:
    """所有异常类可从 scrivai.exceptions 导入。"""
    from scrivai.exceptions import (
        ScrivaiError, PESConfigError, WorkspaceError,
        TrajectoryWriteError, PhaseError, RateLimitError,
    )
    assert issubclass(PESConfigError, ScrivaiError)
    assert issubclass(PhaseError, ScrivaiError)


def test_pes_module_importable() -> None:
    """scrivai.models.pes 可导入全部声明类。"""
    from scrivai.models.pes import (
        ModelConfig, PhaseConfig, PESConfig,
        PhaseTurn, PhaseResult, PESRun,
        HookContext, RunHookContext, PhaseHookContext,
        PromptHookContext, PromptTurnHookContext,
        FailureHookContext, OutputHookContext, CancelHookContext,
    )


def test_workspace_module_importable() -> None:
    from scrivai.models.workspace import (
        WorkspaceSpec, WorkspaceSnapshot, WorkspaceHandle, WorkspaceManager,
    )


def test_knowledge_module_importable() -> None:
    from scrivai.models.knowledge import (
        LibraryEntry, Library, ChunkRef, SearchResult, CollectionInfo,
    )


def test_trajectory_module_importable() -> None:
    from scrivai.models.trajectory import (
        TrajectoryRecord, PhaseRecord, FeedbackRecord,
    )


def test_evolution_module_importable() -> None:
    from scrivai.models.evolution import (
        EvolutionConfig, EvolutionRun, FeedbackExample,
        Evaluator, SkillsRootResolver,
    )


# ────── PESRun.status Literal ──────

def test_pes_run_status_literal_accepts_four_values() -> None:
    from scrivai.models.pes import PESRun
    now = datetime.now(timezone.utc)
    for status in ("running", "completed", "failed", "cancelled"):
        run = PESRun(
            run_id="r1", pes_name="extractor", status=status,
            task_prompt="t", phase_results={}, model_name="m",
            provider="anthropic", sdk_version="1.0", skills_is_dirty=False,
            started_at=now,
        )
        assert run.status == status


def test_pes_run_status_literal_rejects_others() -> None:
    from scrivai.models.pes import PESRun
    now = datetime.now(timezone.utc)
    with pytest.raises(ValidationError):
        PESRun(
            run_id="r1", pes_name="extractor", status="weird",
            task_prompt="t", phase_results={}, model_name="m",
            provider="anthropic", sdk_version="1.0", skills_is_dirty=False,
            started_at=now,
        )


# ────── 9 HookContext + attempt_no 字段 ──────

def test_nine_hook_contexts_exist() -> None:
    """9 个 HookContext 类全部声明。"""
    from scrivai.models.pes import (
        HookContext, RunHookContext, PhaseHookContext,
        PromptHookContext, PromptTurnHookContext,
        FailureHookContext, OutputHookContext, CancelHookContext,
    )
    # design.md §4.1 列了 8 个具体 + 1 个基类 HookContext = 9
    assert all(c is not None for c in [
        HookContext, RunHookContext, PhaseHookContext,
        PromptHookContext, PromptTurnHookContext,
        FailureHookContext, OutputHookContext, CancelHookContext,
    ])


def test_phase_related_hook_contexts_have_attempt_no() -> None:
    """4 个 phase 相关的 hook context 都含 attempt_no 字段。"""
    from scrivai.models.pes import (
        PhaseHookContext, PromptHookContext,
        PromptTurnHookContext, FailureHookContext,
    )
    for cls in [PhaseHookContext, PromptHookContext, PromptTurnHookContext, FailureHookContext]:
        assert "attempt_no" in cls.model_fields, f"{cls.__name__} 缺 attempt_no"


def test_failure_hook_context_extra_fields() -> None:
    """FailureHookContext 额外含 will_retry 和 error_type。"""
    from scrivai.models.pes import FailureHookContext
    assert "will_retry" in FailureHookContext.model_fields
    assert "error_type" in FailureHookContext.model_fields


# ────── PhaseResult round-trip ──────

def test_phase_result_round_trip(sample_phase_result_dict: dict) -> None:
    """model_dump → model_validate 恒等。"""
    from scrivai.models.pes import PhaseResult
    r = PhaseResult.model_validate(sample_phase_result_dict)
    dumped = r.model_dump(mode="json")
    r2 = PhaseResult.model_validate(dumped)
    assert r2.phase == r.phase
    assert r2.attempt_no == r.attempt_no
    assert r2.is_retryable == r.is_retryable
    assert r2.error_type == r.error_type


def test_phase_result_attempt_no_default_zero() -> None:
    from scrivai.models.pes import PhaseResult
    now = datetime.now(timezone.utc)
    r = PhaseResult(
        phase="plan", prompt="p", response_text="r",
        turns=[], produced_files=[], usage={},
        started_at=now, ended_at=now,
    )
    assert r.attempt_no == 0
    assert r.is_retryable is False


# ────── PhaseConfig.required_outputs 多类型 ──────

def test_required_outputs_accepts_str() -> None:
    from scrivai.models.pes import PhaseConfig
    cfg = PhaseConfig(
        name="plan",
        allowed_tools=["Bash"],
        required_outputs=["plan.md", "plan.json"],
    )
    assert cfg.required_outputs == ["plan.md", "plan.json"]


def test_required_outputs_accepts_dict_rule() -> None:
    from scrivai.models.pes import PhaseConfig
    cfg = PhaseConfig(
        name="execute",
        allowed_tools=["Bash"],
        required_outputs=[{"path": "findings/", "min_files": 1, "pattern": "*.json"}],
    )
    assert cfg.required_outputs[0]["path"] == "findings/"


def test_required_outputs_accepts_mixed() -> None:
    from scrivai.models.pes import PhaseConfig
    cfg = PhaseConfig(
        name="plan",
        allowed_tools=["Bash"],
        required_outputs=["plan.md", {"path": "findings/", "min_files": 1, "pattern": "*.json"}],
    )
    assert len(cfg.required_outputs) == 2


# ────── Protocol runtime_checkable ──────

def test_workspace_manager_protocol_runtime_checkable() -> None:
    from scrivai.models.workspace import WorkspaceManager, WorkspaceSpec, WorkspaceHandle
    from typing import runtime_checkable

    class FakeMgr:
        def create(self, spec: WorkspaceSpec) -> WorkspaceHandle: ...
        def archive(self, handle: WorkspaceHandle, success: bool) -> Path: ...
        def cleanup_old(self, days: int = 30) -> None: ...

    fake = FakeMgr()
    assert isinstance(fake, WorkspaceManager)


def test_library_protocol_runtime_checkable() -> None:
    from scrivai.models.knowledge import Library, LibraryEntry

    class FakeLib:
        def add(self, entry_id: str, markdown: str, metadata: dict) -> LibraryEntry: ...
        def get(self, entry_id: str) -> LibraryEntry | None: ...
        def list(self) -> list[str]: ...
        def delete(self, entry_id: str) -> None: ...
        def search(self, query: str, top_k: int = 5, filters: dict | None = None) -> list: ...

    assert isinstance(FakeLib(), Library)


def test_evaluator_protocol_runtime_checkable() -> None:
    from scrivai.models.evolution import Evaluator

    class FakeEval:
        def __call__(self, question: str, predicted: str, ground_truth: str) -> float:
            return 1.0

    assert isinstance(FakeEval(), Evaluator)


def test_skills_root_resolver_protocol_runtime_checkable() -> None:
    from scrivai.models.evolution import SkillsRootResolver

    class FakeResolver:
        def __enter__(self) -> Path: return Path("/tmp")
        def __exit__(self, exc_type, exc_val, exc_tb) -> None: pass

    assert isinstance(FakeResolver(), SkillsRootResolver)


def test_skills_root_resolver_docstring_no_chdir() -> None:
    """SkillsRootResolver Protocol 与方法 docstring 不含 chdir。

    职责边界由 docstring 明确:Resolver 不切 cwd,chdir 由 run_evolution 自己做。
    """
    from scrivai.models.evolution import SkillsRootResolver
    docs = " ".join(filter(None, [
        SkillsRootResolver.__doc__,
        SkillsRootResolver.__enter__.__doc__,
        SkillsRootResolver.__exit__.__doc__,
    ]))
    assert "chdir" not in docs.lower()
```

- [ ] **Step 2: 跑测试,期望 ImportError**

Run: `conda run -n scrivai pytest tests/contract/test_models.py -v`
Expected: 全部测试 ERROR(ImportError: No module named 'scrivai.models')。

### Task 11: 实现 scrivai/models/pes.py

**Files:**
- Create: `scrivai/models/__init__.py` (空文件,占位)
- Create: `scrivai/models/pes.py`

- [ ] **Step 1: 创建占位 __init__.py**

完整内容:
```python
"""Scrivai pydantic + Protocol 数据模型(M0 单一真相)。"""
```

- [ ] **Step 2: 实现 scrivai/models/pes.py 完整内容**

完整内容(对齐 design.md §4.1 字段定义):

```python
"""PES 执行引擎相关 pydantic + 9 个 HookContext。

参考 docs/design.md §4.1 / §4.3。
"""
from __future__ import annotations

from datetime import datetime
from pathlib import Path
from typing import Any, Literal, Optional, Union

from pydantic import BaseModel, ConfigDict, Field


# ────────────────────── 基础配置类 ──────────────────────


class ModelConfig(BaseModel):
    """LLM 模型配置(model id / base_url / api_key 等)。"""
    model_config = ConfigDict(extra="forbid")

    model: str = Field(..., description="模型 id,如 'claude-sonnet-4-6'")
    base_url: Optional[str] = Field(default=None, description="API base URL,None 走 SDK 默认")
    api_key: Optional[str] = Field(default=None, description="API key;通常从 env 读")
    fallback_model: Optional[str] = Field(default=None, description="降级模型 id")


class PhaseConfig(BaseModel):
    """单阶段配置(plan / execute / summarize 任一)。"""
    model_config = ConfigDict(extra="forbid")

    name: str = Field(..., description="阶段名:plan / execute / summarize 或自定义")
    additional_system_prompt: str = Field(default="", description="阶段特定 system prompt 追加")
    allowed_tools: list[str] = Field(..., description="SDK allowed_tools 列表")
    max_turns: int = Field(default=10, description="单次 query 内 Agent 最多交互轮数")
    max_retries: int = Field(default=1, description="Phase 级重试次数(L2 重试)")
    permission_mode: str = Field(default="default", description="SDK permission_mode")
    required_outputs: list[Union[str, dict]] = Field(
        default_factory=list,
        description=(
            "必需产物规则:字符串路径(文件存在即通过)或目录规则 "
            "{'path':'findings/','min_files':1,'pattern':'*.json'}"
        ),
    )


class PESConfig(BaseModel):
    """整个 PES 配置(从 YAML 加载)。"""
    model_config = ConfigDict(extra="forbid")

    name: str = Field(..., description="PES 类型名:extractor / auditor / generator / 自定义")
    display_name: str = Field(default="", description="显示名(给业务层 UI 用)")
    prompt_text: str = Field(..., description="基础 system prompt")
    default_skills: list[str] = Field(default_factory=list, description="默认装入 skills")
    phases: dict[str, PhaseConfig] = Field(..., description="按 phase 名索引的阶段配置")


# ────────────────────── 运行态 ──────────────────────


class PhaseTurn(BaseModel):
    """单次 Agent turn(细粒度轨迹)。"""
    model_config = ConfigDict(extra="forbid")

    turn_index: int = Field(..., description="从 0 开始")
    role: Literal["assistant", "user"] = Field(..., description="user 是 tool result")
    content_type: Literal["text", "tool_use", "tool_result", "thinking"]
    data: dict = Field(..., description="原始消息数据(完整保留)")
    timestamp: datetime


PhaseErrorType = Literal[
    "sdk_rate_limit",
    "sdk_other",
    "max_turns_exceeded",
    "response_parse_error",
    "output_validation_error",
    "cancelled",
    "hook_error",
]


class PhaseResult(BaseModel):
    """单阶段完整结果。"""
    model_config = ConfigDict(extra="forbid")

    phase: Literal["plan", "execute", "summarize"]
    attempt_no: int = Field(default=0, description="本阶段第几次尝试(0 表首次;随 phase 级重试递增)")
    prompt: str = Field(default="", description="最终拼接后的完整 prompt")
    response_text: str = Field(default="", description="LLM 最终 text")
    turns: list[PhaseTurn] = Field(default_factory=list)
    produced_files: list[str] = Field(default_factory=list, description="该阶段写入的文件(相对 working_dir)")
    usage: dict[str, Any] = Field(default_factory=dict, description="SDK token 统计")
    started_at: datetime
    ended_at: Optional[datetime] = None
    error: Optional[str] = None
    error_type: Optional[PhaseErrorType] = Field(default=None, description="错误分类(详见 design §5.3.4)")
    is_retryable: bool = Field(default=False, description="本次失败是否适合 phase 级重试")


PESRunStatus = Literal["running", "completed", "failed", "cancelled"]


class PESRun(BaseModel):
    """一次 PES 执行的完整状态。"""
    model_config = ConfigDict(extra="forbid")

    run_id: str = Field(..., description="调用方指定;workspace 同名;全局唯一")
    pes_name: str = Field(..., description="extractor / auditor / generator / 自定义")
    status: PESRunStatus = Field(default="running", description="当前状态")
    task_prompt: str = Field(..., description="业务层传入的任务描述")
    phase_results: dict[str, PhaseResult] = Field(
        default_factory=dict,
        description="按 phase 名索引的结果(同 phase 多次重试只留最后一次 attempt)",
    )
    final_output: Optional[dict] = Field(default=None, description="summarize 阶段 output.json 解析内容")
    final_output_path: Optional[Path] = Field(default=None, description="working/output.json 绝对路径")
    metadata: dict[str, Any] = Field(default_factory=dict, description="业务扩展字段")
    skills_git_hash: Optional[str] = None
    agents_git_hash: Optional[str] = None
    skills_is_dirty: bool = Field(default=False, description="快照时源 git 有未提交修改则 True")
    model_name: str = Field(..., description="使用的模型 id")
    provider: str = Field(..., description="anthropic / glm / minimax 等")
    sdk_version: str = Field(..., description="claude-agent-sdk 版本号")
    started_at: datetime
    ended_at: Optional[datetime] = None
    error: Optional[str] = None
    error_type: Optional[str] = Field(default=None, description="失败时的错误分类")


# ────────────────────── 9 个 HookContext ──────────────────────


class HookContext(BaseModel):
    """所有 HookContext 的基类,跨插件共享的最小语境。"""
    model_config = ConfigDict(extra="forbid")

    run: PESRun


class RunHookContext(HookContext):
    """before_run / after_run 上下文。"""
    pass


class PhaseHookContext(HookContext):
    """before_phase / after_phase 上下文。"""
    phase: Literal["plan", "execute", "summarize"]
    attempt_no: int = Field(..., description="本阶段第几次尝试")
    phase_result: Optional[PhaseResult] = Field(default=None, description="after_phase 时携带最终结果")


class PromptHookContext(HookContext):
    """before_prompt 上下文,允许修改 prompt。"""
    phase: Literal["plan", "execute", "summarize"]
    attempt_no: int
    prompt: str = Field(..., description="渲染后的完整 prompt(允许 hook 修改)")


class PromptTurnHookContext(HookContext):
    """after_prompt_turn 上下文,每个 SDK turn 触发一次。"""
    phase: Literal["plan", "execute", "summarize"]
    attempt_no: int
    turn: PhaseTurn


class FailureHookContext(HookContext):
    """on_phase_failed 上下文。"""
    phase: Literal["plan", "execute", "summarize"]
    attempt_no: int
    will_retry: bool = Field(..., description="是否会再次尝试本 phase")
    error_type: PhaseErrorType
    phase_result: PhaseResult


class OutputHookContext(HookContext):
    """on_output_written 上下文(仅 summarize 阶段 validate 通过后触发一次)。"""
    output_path: Path
    final_output: dict


class CancelHookContext(HookContext):
    """on_run_cancelled 上下文。"""
    reason: str = Field(default="", description="取消原因(KeyboardInterrupt / asyncio.CancelledError)")
```

- [ ] **Step 3: 跑 test_models.py,期望部分通过(只有 pes 相关的)**

Run: `conda run -n scrivai pytest tests/contract/test_models.py -v`
Expected: `test_pes_module_importable / test_pes_run_status_*` 等 pes 相关测试通过;workspace/knowledge/trajectory/evolution 相关仍 fail(ImportError)。

### Task 12: 实现 scrivai/models/workspace.py

**Files:**
- Create: `scrivai/models/workspace.py`

- [ ] **Step 1: 实现完整内容**

```python
"""Workspace 沙箱相关 pydantic + WorkspaceManager Protocol。

参考 docs/design.md §4.1 / §4.9。
"""
from __future__ import annotations

from datetime import datetime
from pathlib import Path
from typing import Any, Optional, Protocol, runtime_checkable

from pydantic import BaseModel, ConfigDict, Field


class WorkspaceSpec(BaseModel):
    """创建 workspace 的输入规范。"""
    model_config = ConfigDict(extra="forbid")

    run_id: str = Field(..., description="全局唯一;workspace 目录与本 id 同名")
    project_root: Path = Field(..., description="业务项目根(含 skills/ 与 agents/)")
    data_inputs: dict[str, Path] = Field(
        default_factory=dict,
        description="输入文件 logical_name → 源路径 映射;create 时复制到 workspace/data/",
    )
    extra_env: dict[str, str] = Field(default_factory=dict, description="附加环境变量,传给 Agent SDK")
    force: bool = Field(default=False, description="run_id 冲突时:True 覆盖,False 抛 WorkspaceError")


class WorkspaceSnapshot(BaseModel):
    """workspace 快照元信息(写入 meta.json)。"""
    model_config = ConfigDict(extra="forbid")

    run_id: str
    project_root: Path
    skills_git_hash: Optional[str] = Field(default=None, description="快照时 skills 的 git hash")
    agents_git_hash: Optional[str] = None
    snapshot_at: datetime
    extra: dict[str, Any] = Field(default_factory=dict)


class WorkspaceHandle(BaseModel):
    """对一个已创建 workspace 的引用,业务层与 PES 都通过此对象操作。"""
    model_config = ConfigDict(extra="forbid")

    run_id: str
    root_dir: Path = Field(..., description="workspace 根目录(含 working / data / output / logs)")
    working_dir: Path = Field(..., description="Agent 的 cwd(含 .claude/skills+agents)")
    data_dir: Path
    output_dir: Path
    logs_dir: Path
    snapshot: WorkspaceSnapshot


@runtime_checkable
class WorkspaceManager(Protocol):
    """WorkspaceManager Protocol(M0.25 实现)。"""

    def create(self, spec: WorkspaceSpec) -> WorkspaceHandle:
        """按 spec 创建新 workspace;run_id 冲突时按 spec.force 决定行为。"""
        ...

    def archive(self, handle: WorkspaceHandle, success: bool) -> Path:
        """归档 workspace。success=True 打 tar.gz 删原目录;False 写 .failed 标记。"""
        ...

    def cleanup_old(self, days: int = 30) -> None:
        """清理 archives 与 .failed workspace,按 mtime 超过 days 的全删。"""
        ...
```

- [ ] **Step 2: 跑测试**

Run: `conda run -n scrivai pytest tests/contract/test_models.py::test_workspace_module_importable tests/contract/test_models.py::test_workspace_manager_protocol_runtime_checkable -v`
Expected: 通过。

### Task 13: 实现 scrivai/models/knowledge.py

**Files:**
- Create: `scrivai/models/knowledge.py`

- [ ] **Step 1: 实现完整内容**

```python
"""Knowledge Library 相关 pydantic + Library Protocol + qmd re-export。

参考 docs/design.md §4.1 / §4.7。
"""
from __future__ import annotations

from datetime import datetime
from typing import Optional, Protocol, runtime_checkable

from pydantic import BaseModel, ConfigDict, Field

# qmd re-export(身份相等,非副本):scrivai 业务侧通过 scrivai 导入,
# 不需要直接依赖 qmd 名字
from qmd import ChunkRef, SearchResult, CollectionInfo

__all__ = [
    "LibraryEntry",
    "Library",
    # qmd re-export
    "ChunkRef",
    "SearchResult",
    "CollectionInfo",
]


class LibraryEntry(BaseModel):
    """Library 中一条记录(对应 qmd 一个 chunk)。"""
    model_config = ConfigDict(extra="forbid")

    entry_id: str = Field(..., description="collection 内唯一")
    markdown: str = Field(..., description="文本内容")
    metadata: dict = Field(default_factory=dict, description="透传 qmd chunk metadata,无语义解释")
    created_at: Optional[datetime] = None
    updated_at: Optional[datetime] = None


@runtime_checkable
class Library(Protocol):
    """统一的 Library Protocol。RuleLibrary / CaseLibrary / TemplateLibrary 都实现它。"""

    def add(self, entry_id: str, markdown: str, metadata: dict) -> LibraryEntry: ...
    def get(self, entry_id: str) -> Optional[LibraryEntry]: ...
    def list(self) -> list[str]: ...
    def delete(self, entry_id: str) -> None: ...
    def search(
        self, query: str, top_k: int = 5, filters: Optional[dict] = None
    ) -> list[SearchResult]: ...
```

- [ ] **Step 2: 跑测试**

Run: `conda run -n scrivai pytest tests/contract/test_models.py::test_knowledge_module_importable tests/contract/test_models.py::test_library_protocol_runtime_checkable -v`
Expected: 通过。

### Task 14: 实现 scrivai/models/trajectory.py

**Files:**
- Create: `scrivai/models/trajectory.py`

- [ ] **Step 1: 实现完整内容**

```python
"""TrajectoryStore 持久化视图模型。

参考 docs/design.md §4.1 / §4.5。
- TrajectoryRecord:runs 表(可选联查 phases)的只读视图
- PhaseRecord:phases 表一行(同 run_id+phase_name 多 attempt 各一条)
- FeedbackRecord:feedback 表一行
"""
from __future__ import annotations

from datetime import datetime
from typing import Any, Literal, Optional

from pydantic import BaseModel, ConfigDict, Field


class PhaseRecord(BaseModel):
    """phases 表一行(对应 design §4.5 phases schema + design §4.1 PhaseRecord 表)。"""
    model_config = ConfigDict(extra="forbid")

    phase_id: int
    run_id: str
    phase_name: str = Field(..., description="plan / execute / summarize")
    attempt_no: int = Field(default=0, description="同 phase 多次 attempt 区分")
    phase_order: int = Field(..., description="0=plan, 1=execute, 2=summarize")
    prompt: Optional[str] = None
    response_text: Optional[str] = None
    produced_files: list[str] = Field(default_factory=list)
    usage: dict[str, Any] = Field(default_factory=dict)
    error: Optional[str] = None
    error_type: Optional[str] = None
    is_retryable: Optional[bool] = None
    started_at: datetime
    ended_at: Optional[datetime] = None


class TrajectoryRecord(BaseModel):
    """runs 表只读视图(可选联查 phases)。"""
    model_config = ConfigDict(extra="forbid")

    run_id: str
    pes_name: str
    model_name: str
    provider: str
    sdk_version: str
    skills_git_hash: Optional[str] = None
    agents_git_hash: Optional[str] = None
    skills_is_dirty: bool = False
    status: Literal["running", "completed", "failed", "cancelled"]
    task_prompt: str
    runtime_context: Optional[dict] = None
    workspace_archive_path: Optional[str] = None
    final_output: Optional[dict] = None
    error: Optional[str] = None
    error_type: Optional[str] = None
    started_at: datetime
    ended_at: Optional[datetime] = None
    phase_records: list[PhaseRecord] = Field(default_factory=list, description="子表联查(可选)")


class FeedbackRecord(BaseModel):
    """feedback 表一行(对应 design §4.5 feedback schema)。"""
    model_config = ConfigDict(extra="forbid")

    feedback_id: int
    run_id: str
    input_summary: str = Field(..., description="业务层提供的本次 run 输入摘要")
    draft_output: dict = Field(..., description="Agent 原输出")
    final_output: dict = Field(..., description="专家定稿")
    corrections: Optional[list[dict]] = Field(default=None, description="可选结构化 diff")
    review_policy_version: Optional[str] = None
    source: str = Field(default="human_expert", description="human_expert / second_review / gold_set")
    confidence: float = Field(default=1.0, description="0.0-1.0 反馈质量")
    submitted_at: datetime
    submitted_by: Optional[str] = None
```

- [ ] **Step 2: 跑测试**

Run: `conda run -n scrivai pytest tests/contract/test_models.py::test_trajectory_module_importable -v`
Expected: 通过。

### Task 15: 实现 scrivai/models/evolution.py

**Files:**
- Create: `scrivai/models/evolution.py`

- [ ] **Step 1: 实现完整内容**

```python
"""Evolution(EvoSkill 进化)相关 pydantic + Protocol。

参考 docs/design.md §4.6。
- EvolutionConfig / EvolutionRun / FeedbackExample:M0 定义,M2 由 EvolutionTrigger / run_evolution 消费
- Evaluator Protocol:业务层实现的评分函数协议
- SkillsRootResolver Protocol:EvoSkill 的 skills 根目录适配层(M2 实现两个内置)

职责边界:SkillsRootResolver 只准备路径,**不**负责切换 cwd(chdir 由 run_evolution 自己做)。
"""
from __future__ import annotations

from pathlib import Path
from typing import Any, Literal, Optional, Protocol, runtime_checkable

from pydantic import BaseModel, ConfigDict, Field


class FeedbackExample(BaseModel):
    """从 FeedbackRecord 构建进化评测集的中间结构。"""
    model_config = ConfigDict(extra="forbid")

    question: str = Field(..., description="JSON 字符串:{'task_prompt': ..., 'input_summary': ...}")
    ground_truth: str = Field(..., description="final_output 的规范化 JSON 字符串")
    category: str = Field(..., description="分类标签,默认 pes_name,业务可自定义")
    metadata: dict = Field(default_factory=dict, description="透传 review_policy_version/source/confidence/run_id")


class EvolutionConfig(BaseModel):
    """run_evolution 的输入配置。"""
    model_config = ConfigDict(extra="forbid")

    task_name: str
    model: str
    mode: Literal["greedy", "pareto"] = "greedy"
    eval_dataset_csv: Path
    max_iterations: int = 5
    no_improvement_limit: int = 2
    concurrency: int = 4
    train_ratio: float = 0.7
    val_ratio: float = 0.3
    tolerance: float = 0.01
    selection_strategy: Literal["top_k", "pareto_frontier"] = "top_k"
    cache_enabled: bool = True
    cache_dir: Optional[Path] = None
    project_root: Path = Field(..., description="给 SkillsRootResolver 用")


class EvolutionRun(BaseModel):
    """run_evolution 的输出。"""
    model_config = ConfigDict(extra="forbid")

    best_score_base: float
    best_score_evolved: float
    promoted_branch: Optional[str] = Field(default=None, description="最高分超过 base 时填,例 'evo/2026-04-15-123-idx2'")
    candidate_branches: list[str] = Field(default_factory=list)
    iterations_history: list[dict] = Field(default_factory=list)


@runtime_checkable
class Evaluator(Protocol):
    """业务层提供的评分函数。

    返回 0.0 - 1.0,1.0 表示 predicted 与 ground_truth 完全一致。
    """

    def __call__(
        self,
        question: str,
        predicted: str,
        ground_truth: str,
    ) -> float: ...


@runtime_checkable
class SkillsRootResolver(Protocol):
    """解析 EvoSkill 需要看到的 skills 根目录。

    职责范围(单一职责):
    - __enter__: 准备并返回路径 P,使得 P/.claude/skills/ 存在且指向正确内容
    - __exit__: 清理 __enter__ 创建的临时资源(symlink / 临时目录等)

    不负责:
    - 切换进程的工作目录(由 run_evolution 自行处理)
    - 启动 EvoSkill 进程或传参
    """

    def __enter__(self) -> Path:
        """返回路径 P,P/.claude/skills/ 可被 EvoSkill 读到。"""
        ...

    def __exit__(self, exc_type: Any, exc_val: Any, exc_tb: Any) -> None:
        """清理 __enter__ 创建的临时 symlink / 目录。"""
        ...
```

- [ ] **Step 2: 跑测试**

Run: `conda run -n scrivai pytest tests/contract/test_models.py -v`
Expected: 全部 24 个测试通过(若有 fail 检查 docstring 是否漏了 chdir 关键字 / Protocol 方法签名)。

### Task 16: 实现 scrivai/models/__init__.py 聚合

**Files:**
- Modify: `scrivai/models/__init__.py`

- [ ] **Step 1: 改写 __init__.py 完整内容**

```python
"""Scrivai pydantic + Protocol 数据模型(M0 单一真相)。

完整列表参考 docs/design.md §4.1。
"""
from scrivai.models.evolution import (
    Evaluator,
    EvolutionConfig,
    EvolutionRun,
    FeedbackExample,
    SkillsRootResolver,
)
from scrivai.models.knowledge import (
    ChunkRef,
    CollectionInfo,
    Library,
    LibraryEntry,
    SearchResult,
)
from scrivai.models.pes import (
    CancelHookContext,
    FailureHookContext,
    HookContext,
    ModelConfig,
    OutputHookContext,
    PESConfig,
    PESRun,
    PhaseConfig,
    PhaseHookContext,
    PhaseResult,
    PhaseTurn,
    PromptHookContext,
    PromptTurnHookContext,
    RunHookContext,
)
from scrivai.models.trajectory import (
    FeedbackRecord,
    PhaseRecord,
    TrajectoryRecord,
)
from scrivai.models.workspace import (
    WorkspaceHandle,
    WorkspaceManager,
    WorkspaceSnapshot,
    WorkspaceSpec,
)

__all__ = [
    # pes
    "ModelConfig", "PhaseConfig", "PESConfig",
    "PhaseTurn", "PhaseResult", "PESRun",
    "HookContext", "RunHookContext", "PhaseHookContext",
    "PromptHookContext", "PromptTurnHookContext",
    "FailureHookContext", "OutputHookContext", "CancelHookContext",
    # workspace
    "WorkspaceSpec", "WorkspaceSnapshot", "WorkspaceHandle", "WorkspaceManager",
    # knowledge
    "LibraryEntry", "Library", "ChunkRef", "SearchResult", "CollectionInfo",
    # trajectory
    "TrajectoryRecord", "PhaseRecord", "FeedbackRecord",
    # evolution
    "EvolutionConfig", "EvolutionRun", "FeedbackExample",
    "Evaluator", "SkillsRootResolver",
]
```

- [ ] **Step 2: 验证聚合 import**

Run:
```bash
conda run -n scrivai python -c "
from scrivai.models import (
    PESRun, PESConfig, PhaseConfig, PhaseResult, PhaseTurn, ModelConfig,
    HookContext, RunHookContext, PhaseHookContext, PromptHookContext,
    PromptTurnHookContext, FailureHookContext, OutputHookContext, CancelHookContext,
    WorkspaceSpec, WorkspaceSnapshot, WorkspaceHandle, WorkspaceManager,
    LibraryEntry, Library, ChunkRef, SearchResult, CollectionInfo,
    TrajectoryRecord, PhaseRecord, FeedbackRecord,
    EvolutionConfig, EvolutionRun, FeedbackExample, Evaluator, SkillsRootResolver,
)
print('OK')
"
```
Expected: `OK`。

### Task 17: 跑 mypy --strict 验证模型类型

- [ ] **Step 1: 跑严格模式 mypy**

Run: `conda run -n scrivai mypy --strict scrivai/models/`
Expected: `Success: no issues found in N source files`。

- [ ] **Step 2: 若失败,常见排错**

- `Field(default_factory=...)` 类型推断问题 → 显式标 `: list[str] = Field(default_factory=list)`
- `Optional` 与 `None` 默认 → 必须显式 `Optional[X] = None`
- `Protocol` 方法 `...` body → mypy 严格模式下接受,但 docstring 缺失会有 D 类警告(本项目 ruff 不开 D,可忽略)

### Task 18: 写 + 跑 tests/contract/test_reexport.py

**Files:**
- Create: `tests/contract/test_reexport.py`

- [ ] **Step 1: 写完整测试文件**

```python
"""qmd re-export 身份相等契约测试。

scrivai.ChunkRef 应当与 qmd.ChunkRef 是同一个对象(身份相等,非副本)。
"""
from __future__ import annotations

import qmd


def test_chunk_ref_identity_from_models_knowledge() -> None:
    """从 scrivai.models.knowledge 导入的 ChunkRef 与 qmd.ChunkRef 身份相等。"""
    from scrivai.models.knowledge import ChunkRef
    assert ChunkRef is qmd.ChunkRef


def test_search_result_identity_from_models_knowledge() -> None:
    from scrivai.models.knowledge import SearchResult
    assert SearchResult is qmd.SearchResult


def test_collection_info_identity_from_models_knowledge() -> None:
    from scrivai.models.knowledge import CollectionInfo
    assert CollectionInfo is qmd.CollectionInfo


def test_chunk_ref_identity_from_models_aggregate() -> None:
    """从 scrivai.models 聚合层导入的 ChunkRef 也身份相等。"""
    from scrivai.models import ChunkRef
    assert ChunkRef is qmd.ChunkRef


# 注:scrivai.ChunkRef(顶层)的身份相等测试在 Task 26 完成 scrivai/__init__.py 后再加
```

- [ ] **Step 2: 跑测试**

Run: `conda run -n scrivai pytest tests/contract/test_reexport.py -v`
Expected: 4 项全绿。

### Task 19: 写 tests/contract/test_pes_config.py(全部 failing)

**Files:**
- Create: `tests/contract/test_pes_config.py`

- [ ] **Step 1: 写完整测试**

```python
"""T0.3 契约测试:scrivai.pes.config.load_pes_config。"""
from __future__ import annotations

from pathlib import Path

import pytest


def test_load_minimal_extractor_yaml(sample_pes_config_yaml: Path) -> None:
    """fixture 提供的合法 extractor YAML → 返回 PESConfig 实例。"""
    from scrivai.pes.config import load_pes_config
    cfg = load_pes_config(sample_pes_config_yaml)
    assert cfg.name == "extractor"
    assert "plan" in cfg.phases
    assert "execute" in cfg.phases
    assert "summarize" in cfg.phases
    assert cfg.phases["execute"].required_outputs[0]["path"] == "findings/"


def test_env_var_interpolation(tmp_path: Path, env_var_set) -> None:
    """${ENV_VAR} 替换。"""
    env_var_set("SCRIVAI_TEST_PROMPT", "this is from env")
    yaml_text = """\
name: extractor
prompt_text: "${SCRIVAI_TEST_PROMPT}"
phases:
  plan:
    allowed_tools: [Bash]
    required_outputs: [plan.md]
"""
    p = tmp_path / "cfg.yaml"
    p.write_text(yaml_text, encoding="utf-8")

    from scrivai.pes.config import load_pes_config
    cfg = load_pes_config(p)
    assert cfg.prompt_text == "this is from env"


def test_env_var_missing_raises_pes_config_error(tmp_path: Path) -> None:
    """缺失环境变量 → PESConfigError(不让模板字符串原样传下去)。"""
    yaml_text = """\
name: extractor
prompt_text: "${SCRIVAI_DEFINITELY_NOT_SET_XYZ}"
phases:
  plan:
    allowed_tools: [Bash]
    required_outputs: [plan.md]
"""
    p = tmp_path / "cfg.yaml"
    p.write_text(yaml_text, encoding="utf-8")

    from scrivai.exceptions import PESConfigError
    from scrivai.pes.config import load_pes_config

    with pytest.raises(PESConfigError, match="SCRIVAI_DEFINITELY_NOT_SET_XYZ"):
        load_pes_config(p)


def test_schema_validation_error_maps_to_pes_config_error(tmp_path: Path) -> None:
    """缺少必需字段 name → PESConfigError(包装 pydantic ValidationError)。"""
    yaml_text = """\
prompt_text: "no name field"
phases:
  plan:
    allowed_tools: [Bash]
    required_outputs: [plan.md]
"""
    p = tmp_path / "cfg.yaml"
    p.write_text(yaml_text, encoding="utf-8")

    from scrivai.exceptions import PESConfigError
    from scrivai.pes.config import load_pes_config

    with pytest.raises(PESConfigError):
        load_pes_config(p)


def test_yaml_syntax_error_maps_to_pes_config_error(tmp_path: Path) -> None:
    """非法 YAML → PESConfigError。"""
    p = tmp_path / "bad.yaml"
    p.write_text("name: extractor\n  bad indent: : :", encoding="utf-8")

    from scrivai.exceptions import PESConfigError
    from scrivai.pes.config import load_pes_config

    with pytest.raises(PESConfigError):
        load_pes_config(p)


def test_file_not_found_raises_pes_config_error(tmp_path: Path) -> None:
    """文件不存在 → PESConfigError。"""
    from scrivai.exceptions import PESConfigError
    from scrivai.pes.config import load_pes_config

    with pytest.raises(PESConfigError):
        load_pes_config(tmp_path / "does_not_exist.yaml")
```

- [ ] **Step 2: 跑测试,期望 ImportError**

Run: `conda run -n scrivai pytest tests/contract/test_pes_config.py -v`
Expected: 全部 ERROR(ImportError: scrivai.pes.config)。

### Task 20: 实现 scrivai/pes/config.py + scrivai/pes/__init__.py

**Files:**
- Create: `scrivai/pes/__init__.py`
- Create: `scrivai/pes/config.py`

- [ ] **Step 1: scrivai/pes/__init__.py 完整内容**

```python
"""Scrivai PES(Planning-Execute-Summarize)执行引擎。

M0 仅含 PESConfig YAML 加载器;BasePES 在 M0.5 实现。
"""
from scrivai.pes.config import load_pes_config

__all__ = ["load_pes_config"]
```

- [ ] **Step 2: scrivai/pes/config.py 完整内容**

```python
"""PESConfig YAML 加载器。

支持:
- ${ENV_VAR} 环境变量插值(字符串级)
- pydantic schema 校验(失败包装为 PESConfigError)
- YAML 语法错误包装为 PESConfigError
- 文件不存在包装为 PESConfigError
"""
from __future__ import annotations

import os
import re
from pathlib import Path
from typing import Any

import yaml
from pydantic import ValidationError

from scrivai.exceptions import PESConfigError
from scrivai.models.pes import PESConfig

ENV_VAR_PATTERN = re.compile(r"\$\{([A-Z_][A-Z0-9_]*)\}")


def _interpolate_env_vars(node: Any) -> Any:
    """递归把 dict / list / str 中的 ${ENV_VAR} 替换成环境变量值。

    缺失环境变量 → PESConfigError(明确报哪个变量缺)。
    """
    if isinstance(node, str):
        def _replace(match: re.Match[str]) -> str:
            var_name = match.group(1)
            if var_name not in os.environ:
                raise PESConfigError(f"环境变量未设置:{var_name}(在 PESConfig YAML 中引用)")
            return os.environ[var_name]
        return ENV_VAR_PATTERN.sub(_replace, node)
    if isinstance(node, dict):
        return {k: _interpolate_env_vars(v) for k, v in node.items()}
    if isinstance(node, list):
        return [_interpolate_env_vars(v) for v in node]
    return node


def load_pes_config(yaml_path: Path) -> PESConfig:
    """加载 PESConfig YAML 并返回解析后的 PESConfig。

    异常:
      PESConfigError — 文件不存在 / YAML 语法错误 / 环境变量缺失 / pydantic 校验失败
    """
    yaml_path = Path(yaml_path)
    if not yaml_path.exists():
        raise PESConfigError(f"PESConfig YAML 文件不存在:{yaml_path}")

    try:
        raw = yaml.safe_load(yaml_path.read_text(encoding="utf-8"))
    except yaml.YAMLError as e:
        raise PESConfigError(f"PESConfig YAML 语法错误({yaml_path}):{e}") from e

    if not isinstance(raw, dict):
        raise PESConfigError(f"PESConfig YAML 顶层必须是 mapping,得到 {type(raw).__name__}:{yaml_path}")

    interpolated = _interpolate_env_vars(raw)

    try:
        return PESConfig.model_validate(interpolated)
    except ValidationError as e:
        raise PESConfigError(f"PESConfig schema 校验失败({yaml_path}):{e}") from e
```

- [ ] **Step 3: 跑测试**

Run: `conda run -n scrivai pytest tests/contract/test_pes_config.py -v`
Expected: 6 项全绿。

### Task 21: 写 tests/contract/test_evolution_stubs.py(全部 failing)

**Files:**
- Create: `tests/contract/test_evolution_stubs.py`

- [ ] **Step 1: 写完整测试**

```python
"""T0.18 契约测试:Evolution stubs 与 SkillsRootResolver Protocol。"""
from __future__ import annotations

import asyncio

import pytest


def test_evolution_trigger_importable_from_top_level() -> None:
    """scrivai.EvolutionTrigger 顶层可导入。"""
    from scrivai import EvolutionTrigger
    assert EvolutionTrigger is not None


def test_run_evolution_importable_from_top_level() -> None:
    """scrivai.run_evolution 顶层可导入。"""
    from scrivai import run_evolution
    assert callable(run_evolution)


def test_evolution_trigger_init_raises_not_implemented_m2() -> None:
    """EvolutionTrigger() 抛 NotImplementedError 且 message 含 'M2'。"""
    from scrivai import EvolutionTrigger
    with pytest.raises(NotImplementedError, match="M2"):
        EvolutionTrigger()


def test_run_evolution_raises_not_implemented_m2() -> None:
    """run_evolution() 是 async 函数,await 时抛 NotImplementedError。"""
    from scrivai import run_evolution
    with pytest.raises(NotImplementedError, match="M2"):
        asyncio.run(run_evolution())


def test_evolution_package_importable() -> None:
    """scrivai.evolution 包可 import,且不抛异常。"""
    import scrivai.evolution
    assert scrivai.evolution is not None
```

- [ ] **Step 2: 跑测试,期望 ImportError**

Run: `conda run -n scrivai pytest tests/contract/test_evolution_stubs.py -v`
Expected: 全部 ERROR(scrivai.EvolutionTrigger 不存在 — 因为 scrivai/__init__.py 还没暴露)。

### Task 22: 实现 scrivai/evolution/ 占位

**Files:**
- Create: `scrivai/evolution/__init__.py`
- Create: `scrivai/evolution/trigger.py`
- Create: `scrivai/evolution/runner.py`
- Create: `scrivai/evolution/evaluator.py`

- [ ] **Step 1: scrivai/evolution/__init__.py**

```python
"""Scrivai Evolution — Skill 进化(M2 实现)。

M0 仅占位,真实实现在 M2 T2.1-T2.4。
"""
from scrivai.evolution.runner import run_evolution
from scrivai.evolution.trigger import EvolutionTrigger

__all__ = ["EvolutionTrigger", "run_evolution"]
```

- [ ] **Step 2: scrivai/evolution/trigger.py**

```python
"""EvolutionTrigger:从 TrajectoryStore 构建进化评测集(M2 实现)。"""
from __future__ import annotations

from typing import Any


class EvolutionTrigger:
    """从 TrajectoryStore 构建进化评测集。

    M2 T2.2 实现完整逻辑;M0 仅占位以保证顶层 import 可用。
    """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        raise NotImplementedError("M2 实现")

    def has_enough_data(self) -> bool:
        raise NotImplementedError("M2 实现")

    def build_eval_dataset(self, *args: Any, **kwargs: Any) -> Any:
        raise NotImplementedError("M2 实现")
```

- [ ] **Step 3: scrivai/evolution/runner.py**

```python
"""run_evolution:EvoSkill 五阶段进化(M2 实现)。"""
from __future__ import annotations

from typing import Any


async def run_evolution(*args: Any, **kwargs: Any) -> Any:
    """EvoSkill 五阶段进化入口。

    M2 T2.3 实现完整逻辑;M0 仅占位以保证顶层 import 可用。
    """
    raise NotImplementedError("M2 实现")
```

- [ ] **Step 4: scrivai/evolution/evaluator.py(空占位)**

```python
"""Evaluator 内置实现(M2 T2.4 提供 IoUEvaluator / ExactMatchEvaluator 等)。"""
```

- [ ] **Step 5: 验证 evolution 包 import**

Run:
```bash
conda run -n scrivai python -c "
import scrivai.evolution
from scrivai.evolution import EvolutionTrigger, run_evolution
print('OK')
"
```
Expected: `OK`。

### Task 23: 改写 scrivai/__init__.py 至 M0 终态

**Files:**
- Modify: `scrivai/__init__.py`

- [ ] **Step 1: 完整改写**

```python
"""Scrivai v3 — Claude Agent 编排框架(M0 地基层)。

完整 Public API 在 M0.75 冻结;本里程碑只暴露:
- qmd 基础类型 re-export(身份相等于 qmd.X)
- Evolution 占位类(M2 实现)

参考 docs/design.md §4.1。
"""
from qmd import ChunkRef, CollectionInfo, SearchResult

from scrivai.evolution import EvolutionTrigger, run_evolution

__all__ = [
    "ChunkRef",
    "CollectionInfo",
    "SearchResult",
    "EvolutionTrigger",
    "run_evolution",
]
```

- [ ] **Step 2: 跑 evolution stubs 契约测试**

Run: `conda run -n scrivai pytest tests/contract/test_evolution_stubs.py -v`
Expected: 5 项全绿。

- [ ] **Step 3: 加一个顶层 ChunkRef 身份相等测试**

在 `tests/contract/test_reexport.py` 末尾追加:

```python


def test_chunk_ref_identity_from_scrivai_top_level() -> None:
    """scrivai.ChunkRef is qmd.ChunkRef(顶层 re-export 身份相等)。"""
    import scrivai
    assert scrivai.ChunkRef is qmd.ChunkRef


def test_search_result_identity_from_scrivai_top_level() -> None:
    import scrivai
    assert scrivai.SearchResult is qmd.SearchResult


def test_collection_info_identity_from_scrivai_top_level() -> None:
    import scrivai
    assert scrivai.CollectionInfo is qmd.CollectionInfo
```

- [ ] **Step 4: 跑全部 reexport 测试**

Run: `conda run -n scrivai pytest tests/contract/test_reexport.py -v`
Expected: 7 项全绿(原 4 + 新 3)。

### Task 24: 全量 M0 DoD 验证

- [ ] **Step 1: 跑全部契约测试**

Run: `conda run -n scrivai pytest tests/contract/ -v`
Expected: 全绿(test_models 24 + test_reexport 7 + test_pes_config 6 + test_evolution_stubs 5 ≈ 42 项,具体数字以代码为准)。

- [ ] **Step 2: mypy --strict**

Run: `conda run -n scrivai mypy --strict scrivai/models/`
Expected: `Success: no issues found in 6 source files`(__init__ + 5 子模块)。

- [ ] **Step 3: legacy 符号清洁性**

Run: `git grep -E "LLMClient|AuditEngine|GenerationEngine|ProjectConfig|KnowledgeStore" -- scrivai/ tests/`
Expected: 零输出。

- [ ] **Step 4: 顶层 import smoke**

Run:
```bash
conda run -n scrivai python -c "
from scrivai.models.pes import (
    PESRun, PESConfig, PhaseConfig, PhaseResult, PhaseTurn, ModelConfig,
    HookContext, RunHookContext, PhaseHookContext, PromptHookContext,
    PromptTurnHookContext, FailureHookContext, OutputHookContext, CancelHookContext,
)
from scrivai.models.evolution import (
    SkillsRootResolver, EvolutionConfig, EvolutionRun, FeedbackExample, Evaluator,
)
import scrivai
from scrivai import EvolutionTrigger, run_evolution, ChunkRef, SearchResult, CollectionInfo
import qmd
assert scrivai.ChunkRef is qmd.ChunkRef
print('OK')
"
```
Expected: `OK`。

- [ ] **Step 5: stub 行为 smoke**

Run:
```bash
conda run -n scrivai python -c "
import asyncio
from scrivai import run_evolution
try:
    asyncio.run(run_evolution())
except NotImplementedError as e:
    assert 'M2' in str(e)
    print('OK')
"
```
Expected: `OK`。

- [ ] **Step 6: ruff format + lint**

Run:
```bash
conda run -n scrivai ruff check . --fix
conda run -n scrivai ruff format .
```
Expected: 第一条若有自动修复会输出 fixed N issues;第二条输出 X file(s) reformatted。

- [ ] **Step 7: 重跑契约测试确保 ruff 修改未破坏**

Run: `conda run -n scrivai pytest tests/contract/ -v`
Expected: 全绿。

### Task 25: 提交 Commit 3

- [ ] **Step 1: 检查 stage 状态**

Run: `git status --short`
Expected: 大量 `??`(新建文件)与可能的 `M`(ruff format 后的文件):
```
?? scrivai/exceptions.py
?? scrivai/models/...
?? scrivai/pes/...
?? scrivai/evolution/...
M  scrivai/__init__.py
?? tests/contract/...
```

- [ ] **Step 2: 暂存所有 M0 代码**

Run:
```bash
git add scrivai/exceptions.py \
        scrivai/models/ \
        scrivai/pes/ \
        scrivai/evolution/ \
        scrivai/__init__.py \
        tests/contract/
```

- [ ] **Step 3: 验证 stage 内容**

Run: `git diff --cached --stat`
Expected: 列出所有新建文件 + scrivai/__init__.py 的修改。

- [ ] **Step 4: 提交 Commit 3**

Run:
```bash
git commit -m "$(cat <<'EOF'
feat: add M0 models + config + evolution stubs + contract tests

- exceptions.py: 5 类异常前置声明(PESConfigError 等)
- models/{pes,workspace,knowledge,trajectory,evolution}.py:全部 pydantic + Protocol(T0.2)
- models/__init__.py:聚合所有符号
- pes/config.py:load_pes_config YAML loader,支持 ${ENV_VAR} 插值(T0.3)
- evolution/{trigger,runner,evaluator}.py:M2 占位,调用抛 NotImplementedError("M2 实现")(T0.18)
- __init__.py:顶层 re-export qmd 基础类型 + EvolutionTrigger / run_evolution
- tests/contract/{conftest,test_models,test_reexport,test_pes_config,test_evolution_stubs}.py:M0 全部契约测试
EOF
)"
```
Expected: 一行 `[feat/scrivai-m0-foundation <hash>] feat: add M0 models + config + evolution stubs + contract tests`。

- [ ] **Step 5: 最终 git log 确认**

Run: `git log --oneline -5`
Expected:
```
<hash5> feat: add M0 models + config + evolution stubs + contract tests
<hash4> refactor: clean legacy v2 code for v3 foundation
<hash3> docs: finalize v3 spec
519efdc docs: add M0 execution design spec
accd3d4 docs: 拆分 M0 为 M0 / M0.25 / M0.5 / M0.75 四个独立可验证子里程碑
```

- [ ] **Step 6: 收尾验证**

Run: `conda run -n scrivai pytest tests/contract/ -v && conda run -n scrivai mypy --strict scrivai/models/`
Expected: 测试全绿 + mypy 通过。

---

## 完成判定(M0 DoD 总和)

- [x] legacy 清理干净(`git grep` 验证)
- [x] `mypy --strict scrivai/models/` 通过
- [x] 4 个契约测试集全绿(`tests/contract/test_{models,reexport,pes_config,evolution_stubs}.py`)
- [x] `from scrivai.models.pes import ...` 9 个 HookContext + 6 个 pydantic 全部可导入
- [x] `from scrivai.models.evolution import SkillsRootResolver, ...` 可导入
- [x] `scrivai.EvolutionTrigger / run_evolution` 顶层可 import,调用抛 `NotImplementedError("M2 实现")`
- [x] `scrivai.ChunkRef is qmd.ChunkRef` 身份相等

---

## Self-Review Notes

**Spec 覆盖**:
- spec §3.1 Commit 1 → Task 1 ✅
- spec §3.2 Commit 2 → Task 2-7 ✅
- spec §3.3.1 exceptions.py → Task 8 ✅
- spec §3.3.2 models/ → Task 11-16 ✅(按文件分 5 个 task + 聚合)
- spec §3.3.3 pes/config.py → Task 19-20 ✅
- spec §3.3.4 evolution/ stubs → Task 21-22 ✅
- spec §3.3.5 scrivai/__init__.py 终态 → Task 23 ✅
- spec §3.3.6 contract tests → Task 9, 10, 18, 19, 21, 23 ✅
- spec §4 验证计划 → Task 24 ✅
- spec §5 风险识别(qmd 0.1.2 升级)→ pyproject 已升 0.1.2 ✅(Task 4),`[tool.mypy] plugins=["pydantic.mypy"]` 已加 ✅

**Spec 修正项已落地**:
- spec §3.2.2 路径错误(`tests/e2e/test_doc_pipeline_e2e.py`) → 已在 Task 3 用正确路径 `tests/integration/test_doc_pipeline_e2e.py`
- spec §3.2.4 qmd 版本 → Task 4 改 `qmd>=0.1.2`

**Type 一致性核查**:
- `PESRunStatus = Literal["running","completed","failed","cancelled"]` 在 pes.py 与 trajectory.py 用同一拼写 ✅
- `PhaseErrorType` 在 pes.py 定义,FailureHookContext 中复用 ✅
- `PhaseResult.attempt_no: int = 0` 与契约测试 `test_phase_result_attempt_no_default_zero` 一致 ✅
- `WorkspaceManager.create / archive / cleanup_old` 签名在 `test_workspace_manager_protocol_runtime_checkable` 与 workspace.py Protocol 一致 ✅
- `Library.add/get/list/delete/search` 签名在测试与 knowledge.py Protocol 一致 ✅

**Placeholder 扫描**:
- 无 TBD / TODO / "implement later"
- 所有 step 都给出可执行命令或完整代码块
- 函数签名在前置 task 与后续 task 间对齐
