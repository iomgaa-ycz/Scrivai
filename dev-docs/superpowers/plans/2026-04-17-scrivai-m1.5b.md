# M1.5b — Library + IO 边缘 DoD 补齐 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline 模式,每 commit 前开 code-reviewer subagent)。Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐 TD.md T1.8 边缘 DoD(表格 docx 保留 / docxtpl `{% for %}` 循环 / metadata filter 搜索),对齐 `design.md §4.8` 的 pdf_to_markdown 到 MonkeyOCR 现实,关闭 M1 开发线(T1.7 已满足,无需改动)。

**Architecture:** **纯测试强化 + 文档对齐**,**零 `scrivai/{knowledge,io}/` 生产代码变更**(M0.75 commits 已落地真实接入)。新增 `scripts/` 下两个 fixture 生成脚本 + `tests/fixtures/io_samples/` 二进制 `.docx` fixture(入 git);`test_io_smoke.py` + `test_libraries.py` 各新增覆盖;`design.md §4.8` + `docs/TD.md` T1.7/T1.8 同步偏离注解。执行风格沿用 M1.5a:Inline + 每 commit 前开 code-reviewer subagent。

**Tech Stack:** Python 3.11, python-docx(fixture 构造), docxtpl(循环模板验证), pandoc 2.5(表格转换), qmd 0.1.2(metadata filter), pytest.

**前置(人工一次性)**: `sudo apt install pandoc` — **已装**(pandoc 2.5 @ /usr/bin/pandoc)。

**分支**: `feat/scrivai-m1.5b-library-io` 从 main 拉(main HEAD = `6af2f29`)。

**Commit 计划**(2 个 + finishing):
1. **Commit 1**: `test: add fixtures + coverage for T1.8 edge DoD (M1.5b)` — 脚本 + fixture + 4 个新测试
2. **Commit 2**: `docs: align design.md §4.8 to MonkeyOCR + annotate TD.md T1.7/T1.8` — 文档对齐
3. **Finishing**: 最终验证 + `superpowers:finishing-a-development-branch`(用户选 merge / PR / keep / discard)

---

## File Structure

```
scripts/                                NEW 目录
├── make_table_sample.py                用 python-docx 造 2x3 表格 docx
└── make_loop_template.py               用 python-docx + XML 后处理造 docxtpl {% for %} 模板

tests/fixtures/io_samples/              NEW 目录
├── README.md                           fixture 生成路径与用途说明
├── table_sample.docx                   bin fixture(约 7 KB)
└── loop_template.docx                  bin fixture(约 7 KB)

tests/contract/test_io_smoke.py         MODIFY 追加:
  ├── test_docx_to_markdown_preserves_table
  ├── test_docx_renderer_list_placeholders_in_loop
  └── test_docx_renderer_render_loop

tests/contract/test_libraries.py        MODIFY 追加:
  └── test_search_with_metadata_filter

docs/design.md                          MODIFY §4.8(~L1077 + 一段说明)
docs/TD.md                              MODIFY T1.7/T1.8 末尾追加「实施偏离」段
```

**边界约定**(防止 scope 漂):
- **不改** `scrivai/knowledge/*.py` / `scrivai/io/*.py` / `scrivai/models/`
- **不改** 现有 test 的既有断言(只追加新 test 函数)
- **不引入** docling / langchain / 其他新依赖
- **不动** 现有 MonkeyOCR `base_url` 默认值(只改 design.md 的描述)

---

## Task 1: 前置环境校验 + 拉分支

**Files:** (无文件变更)

- [ ] **Step 1: 确认 main 状态与 pandoc**

```bash
git status
git branch --show-current
which pandoc && pandoc --version | head -1
```

Expected:
```
nothing to commit, working tree clean
main
/usr/bin/pandoc
pandoc 2.5
```

- [ ] **Step 2: 从 main 拉新分支**

```bash
git checkout -b feat/scrivai-m1.5b-library-io
git log --oneline -1
```

Expected: `6af2f29 style: ruff format + isort across scrivai/pes and tests/contract`

- [ ] **Step 3: 跑一次基线快速确认(只跑 io+libraries 两个文件,~20s)**

```bash
conda run -n scrivai pytest tests/contract/test_libraries.py tests/contract/test_io_smoke.py -v 2>&1 | tail -8
```

Expected: `14 passed, 1 skipped`(MonkeyOCR smoke 网络不可达为正常)。

---

## Task 2: 构造 `table_sample.docx` fixture

**Files:**
- Create: `scripts/make_table_sample.py`
- Create: `tests/fixtures/io_samples/table_sample.docx`(由脚本生成,入 git)

- [ ] **Step 1: 写构造脚本**

```python
# scripts/make_table_sample.py
"""一次性构造 tests/fixtures/io_samples/table_sample.docx。

内容:标题 + 前文段 + 2 列 3 行表(含表头)+ 后文段。
用于 test_docx_to_markdown_preserves_table 验证 pandoc 保留表格结构。
"""

from __future__ import annotations

from pathlib import Path

from docx import Document

FIXTURE = (
    Path(__file__).resolve().parents[1]
    / "tests"
    / "fixtures"
    / "io_samples"
    / "table_sample.docx"
)


def build() -> None:
    FIXTURE.parent.mkdir(parents=True, exist_ok=True)
    doc = Document()
    doc.add_heading("Substation Inspection Report", level=1)
    doc.add_paragraph("Pre-table narrative paragraph.")

    table = doc.add_table(rows=3, cols=2)
    table.style = "Table Grid"
    table.rows[0].cells[0].text = "Checkpoint"
    table.rows[0].cells[1].text = "Verdict"
    table.rows[1].cells[0].text = "Insulation resistance"
    table.rows[1].cells[1].text = "Pass"
    table.rows[2].cells[0].text = "Grounding continuity"
    table.rows[2].cells[1].text = "Fail"

    doc.add_paragraph("Post-table conclusion.")
    doc.save(FIXTURE)
    print(f"built {FIXTURE} ({FIXTURE.stat().st_size} B)")


if __name__ == "__main__":
    build()
```

- [ ] **Step 2: 运行脚本产出 fixture**

```bash
conda run -n scrivai python scripts/make_table_sample.py
ls -la tests/fixtures/io_samples/table_sample.docx
```

Expected: 打印 `built .../table_sample.docx (约 37000 B)`(python-docx 默认模板有约 35KB 开销),文件存在。

- [ ] **Step 3: 快速人工校验 fixture 确实含表格**

```bash
conda run -n scrivai python -c "
from docx import Document
d = Document('tests/fixtures/io_samples/table_sample.docx')
print('paragraphs:', len(d.paragraphs))
print('tables:', len(d.tables))
print('first table rows x cols:', len(d.tables[0].rows), 'x', len(d.tables[0].columns))
"
```

Expected:
```
paragraphs: 3
tables: 1
first table rows x cols: 3 x 2
```

---

## Task 3: 构造 `loop_template.docx` fixture(XML 后处理)

**Files:**
- Create: `scripts/make_loop_template.py`
- Create: `tests/fixtures/io_samples/loop_template.docx`(由脚本生成,入 git)

**背景说明**: docxtpl 的 `{% for %}` jinja 标签必须在单一 `<w:r>` 内 — python-docx 可能按 auto-formatting 把字符拆到多 `<w:r>`。规避方式:先写入唯一占位字符串(`__TAG_FOR__` 等),保存后 unzip docx,对 `word/document.xml` 字符串替换,再重打包。docxtpl 社区推荐此法。

- [ ] **Step 1: 写构造脚本**

```python
# scripts/make_loop_template.py
"""一次性构造 tests/fixtures/io_samples/loop_template.docx。

docxtpl 的 {% for %} 标签必须在单一 <w:r> 内,python-docx 生成时可能拆 run。
规避:先写占位串(__TAG_FOR__ / __TAG_ITEM_NAME__ / __TAG_ENDFOR__),
保存后 unzip docx,字符串替换 word/document.xml,再重新打包。

目标模板:
  Report: {{ project_name }}

  {% for item in items %}
  {{ item.name }}
  {% endfor %}
"""

from __future__ import annotations

import zipfile
from pathlib import Path

from docx import Document

FIXTURE = (
    Path(__file__).resolve().parents[1]
    / "tests"
    / "fixtures"
    / "io_samples"
    / "loop_template.docx"
)

PLACEHOLDER_FOR = "__TAG_FOR__"
PLACEHOLDER_ITEM = "__TAG_ITEM_NAME__"
PLACEHOLDER_ENDFOR = "__TAG_ENDFOR__"

REPLACEMENTS: dict[str, str] = {
    PLACEHOLDER_FOR: "{% for item in items %}",
    PLACEHOLDER_ITEM: "{{ item.name }}",
    PLACEHOLDER_ENDFOR: "{% endfor %}",
}


def build() -> None:
    FIXTURE.parent.mkdir(parents=True, exist_ok=True)
    doc = Document()
    doc.add_heading("Report: {{ project_name }}", level=1)
    doc.add_paragraph(PLACEHOLDER_FOR)
    doc.add_paragraph(PLACEHOLDER_ITEM)
    doc.add_paragraph(PLACEHOLDER_ENDFOR)

    tmp = FIXTURE.with_suffix(".tmp.docx")
    doc.save(tmp)

    # XML 后处理:解压 → 替换 → 重新打包
    with zipfile.ZipFile(tmp, "r") as zin:
        names = zin.namelist()
        blobs = {n: zin.read(n) for n in names}

    xml = blobs["word/document.xml"].decode("utf-8")
    for src, dst in REPLACEMENTS.items():
        if src not in xml:
            raise RuntimeError(
                f"占位符 {src!r} 未出现在 document.xml — python-docx 可能已拆 run"
            )
        xml = xml.replace(src, dst)
    blobs["word/document.xml"] = xml.encode("utf-8")

    with zipfile.ZipFile(FIXTURE, "w", zipfile.ZIP_DEFLATED) as zout:
        for n in names:
            zout.writestr(n, blobs[n])

    tmp.unlink()
    print(f"built {FIXTURE} ({FIXTURE.stat().st_size} B)")


if __name__ == "__main__":
    build()
```

- [ ] **Step 2: 运行脚本产出 fixture**

```bash
conda run -n scrivai python scripts/make_loop_template.py
ls -la tests/fixtures/io_samples/loop_template.docx
```

Expected: `built .../loop_template.docx (约 36000 B)`,文件存在。

- [ ] **Step 3: 用 docxtpl 本地渲染一次确认模板可解析**

```bash
conda run -n scrivai python -c "
from docxtpl import DocxTemplate
tpl = DocxTemplate('tests/fixtures/io_samples/loop_template.docx')
tpl.render({'project_name': 'test-proj', 'items': [{'name':'a'},{'name':'b'}]})
tpl.save('/tmp/loop_render_test.docx')
print('render OK')
from docx import Document
d = Document('/tmp/loop_render_test.docx')
text = '\n'.join(p.text for p in d.paragraphs)
print('rendered text:')
print(text)
"
```

Expected: `render OK` + 输出文本包含 `test-proj` 和 `a`、`b` 两行(或类似布局)。若报 TemplateSyntaxError,回 Step 1 检查 placeholder 替换。

---

## Task 4: `tests/fixtures/io_samples/README.md`

**Files:**
- Create: `tests/fixtures/io_samples/README.md`

- [ ] **Step 1: 写 README**

```markdown
# IO samples fixtures

本目录的 `.docx` 二进制 fixture 由 `scripts/` 下的一次性构造脚本生成,入 git(~70 KB 合计)。

| 文件 | 生成脚本 | 用途 |
|---|---|---|
| `table_sample.docx` | `scripts/make_table_sample.py` | 验证 `docx_to_markdown` 保留表格结构(pandoc 输出) |
| `loop_template.docx` | `scripts/make_loop_template.py` | 验证 `DocxRenderer` 支持 docxtpl `{% for %}` 循环模板 |

## 重新生成

若 fixture 损坏或需更新,删掉重跑脚本:

```bash
rm tests/fixtures/io_samples/*.docx
conda run -n scrivai python scripts/make_table_sample.py
conda run -n scrivai python scripts/make_loop_template.py
```

## 为什么 `loop_template.docx` 需要 XML 后处理

docxtpl 的 `{% for %}` jinja 标签要求整个标签在同一 `<w:r>` 内。python-docx 写入字符串时可能因 auto-formatting 把字符拆到多 run,导致 docxtpl 解析失败。脚本采取 **占位字符串 + XML 字符串替换** 的社区规避方案:先写 `__TAG_FOR__` 等唯一占位,保存后解压 docx,对 `word/document.xml` 做 replace,再重新打包。
```

- [ ] **Step 2: 确认文件落位**

```bash
ls -la tests/fixtures/io_samples/
```

Expected:
```
README.md
loop_template.docx
table_sample.docx
```

---

## Task 5: `test_docx_to_markdown_preserves_table`

**Files:**
- Modify: `tests/contract/test_io_smoke.py`(在文件末尾追加新 test)

- [ ] **Step 1: 写失败测试(追加到文件末尾)**

追加到 `tests/contract/test_io_smoke.py` 的末尾:

```python


# ─── M1.5b T1.8 edge DoD ───────────────────────────────────


FIXTURES_DIR = Path(__file__).resolve().parents[1] / "fixtures" / "io_samples"


@pytest.mark.skipif(not shutil.which("pandoc"), reason="需要 pandoc 二进制")
def test_docx_to_markdown_preserves_table() -> None:
    """pandoc 把含表格的 docx 转 markdown,保留表格结构(内容 + 表格语法)。"""
    from scrivai.io import docx_to_markdown

    fixture = FIXTURES_DIR / "table_sample.docx"
    assert fixture.is_file(), f"fixture 不存在:{fixture}"

    md = docx_to_markdown(fixture)
    # 内容必须全部出现
    assert "Checkpoint" in md
    assert "Verdict" in md
    assert "Insulation resistance" in md
    assert "Pass" in md
    assert "Grounding continuity" in md
    assert "Fail" in md
    # pandoc 默认 markdown 会把表格渲染成 pipe 表格 或 grid 表格;任一即可
    assert ("|" in md) or ("+-" in md), f"pandoc 输出应保留表格结构;实际:{md[:500]}"
```

- [ ] **Step 2: 跑测试确认通过**

```bash
conda run -n scrivai pytest tests/contract/test_io_smoke.py::test_docx_to_markdown_preserves_table -v
```

Expected: `PASSED`。若失败,检查 pandoc 版本(2.5 已验证 OK)与 fixture 是否含表格(回 Task 2 Step 3)。

---

## Task 6: docxtpl 循环模板两个新测试

**Files:**
- Modify: `tests/contract/test_io_smoke.py`(继续追加)

- [ ] **Step 1: 追加两个测试**

继续追加到 `tests/contract/test_io_smoke.py` 末尾(紧跟 Task 5 的 test 后):

```python


def test_docx_renderer_list_placeholders_in_loop() -> None:
    """loop_template.docx 含 `{{ project_name }}` 简单占位 + `{{ item.name }}` 复杂占位;
    现行 list_placeholders 正则 `[a-zA-Z_][a-zA-Z0-9_]*` 只识别简单标识符,
    所以只断言 project_name 必出现,item.name 因含点号不入列表(行为契约)。"""
    from scrivai.io import DocxRenderer

    fixture = FIXTURES_DIR / "loop_template.docx"
    assert fixture.is_file(), f"fixture 不存在:{fixture}"

    renderer = DocxRenderer(fixture)
    names = renderer.list_placeholders()
    assert "project_name" in names, f"project_name 必在;actual={names}"
    # item.name 因含 "." 不被当前正则捕获;不做否定断言(实现可能升级)


def test_docx_renderer_render_loop(tmp_path: Path) -> None:
    """loop_template.docx 渲染 3 item 后,每 item 应在输出中各出现一次。"""
    from docx import Document as _Doc

    from scrivai.io import DocxRenderer

    fixture = FIXTURES_DIR / "loop_template.docx"
    out = tmp_path / "loop_rendered.docx"

    renderer = DocxRenderer(fixture)
    ctx: dict = {
        "project_name": "Sub-7",
        "items": [{"name": "alpha"}, {"name": "beta"}, {"name": "gamma"}],
    }
    result = renderer.render(context=ctx, output_path=out)
    assert result == out
    assert out.is_file()
    assert out.stat().st_size > 0

    rendered = _Doc(str(out))
    text = "\n".join(p.text for p in rendered.paragraphs)
    assert "Sub-7" in text, f"project_name 未渲染;text:\n{text}"
    for name in ("alpha", "beta", "gamma"):
        assert name in text, f"item {name!r} 未渲染;text:\n{text}"
```

- [ ] **Step 2: 跑两个新测试**

```bash
conda run -n scrivai pytest tests/contract/test_io_smoke.py::test_docx_renderer_list_placeholders_in_loop tests/contract/test_io_smoke.py::test_docx_renderer_render_loop -v
```

Expected: 两个都 `PASSED`。若 `test_docx_renderer_render_loop` 失败提示 jinja 语法错,回 Task 3 检查 XML 后处理的 fixture 是否正确。

- [ ] **Step 3: 跑整个 test_io_smoke.py 确保无回归**

```bash
conda run -n scrivai pytest tests/contract/test_io_smoke.py -v --no-header 2>&1 | tail -20
```

Expected: `10 passed, 1 skipped`(原 7 passed + 3 新 + 1 MonkeyOCR skip 不变)。

---

## Task 7: `test_search_with_metadata_filter`

**Files:**
- Modify: `tests/contract/test_libraries.py`(在文件末尾追加新 test)

- [ ] **Step 1: 确认 qmd 支持 filter**

```bash
conda run -n scrivai python -c "
import qmd
c = qmd.connect(':memory:')
coll = c.collection('x')
coll.add_document('a', '政府采购法', {'src':'law-A'})
coll.add_document('b', '招投标法', {'src':'law-B'})
r = coll.hybrid_search('法', top_k=5, filters={'src':'law-A'})
assert all(x.metadata.get('src') == 'law-A' for x in r), f'filter 未生效:{r}'
print('qmd filter OK; len:', len(r))
" 2>&1 | grep -v "INFO\|embedding" | tail -3
```

Expected: `qmd filter OK; len: 1`。

- [ ] **Step 2: 追加测试**

追加到 `tests/contract/test_libraries.py` 的末尾(在最后一个 test 函数后):

```python


def test_search_with_metadata_filter(libraries) -> None:
    """hybrid_search 的 filters 参数:透传 qmd;只返回匹配 metadata 的 chunk。

    规格参考 docs/design.md §4.7 + TD.md T1.7 DoD「get/list/delete 通过 metadata filters」。
    """
    rules, _, _ = libraries
    rules.add(entry_id="f-a", markdown="围标串标的认定标准", metadata={"src": "law-A"})
    rules.add(entry_id="f-b", markdown="围标串标的监管责任", metadata={"src": "law-B"})
    rules.add(entry_id="f-c", markdown="政府采购供应商资格", metadata={"src": "law-A"})

    results_a = rules.search(query="围标", top_k=5, filters={"src": "law-A"})
    assert len(results_a) >= 1, "filter src=law-A 应至少命中 f-a"
    for r in results_a:
        assert r.metadata.get("src") == "law-A", (
            f"filter 未生效;得到 {r.metadata}"
        )

    results_b = rules.search(query="围标", top_k=5, filters={"src": "law-B"})
    assert len(results_b) >= 1, "filter src=law-B 应至少命中 f-b"
    for r in results_b:
        assert r.metadata.get("src") == "law-B"
```

- [ ] **Step 3: 跑新测试**

```bash
conda run -n scrivai pytest tests/contract/test_libraries.py::test_search_with_metadata_filter -v
```

Expected: `PASSED`。若失败,检查 qmd 版本(0.1.2 已验证)与 Step 1 的探测输出。

- [ ] **Step 4: 跑整个 test_libraries.py 确保无回归**

```bash
conda run -n scrivai pytest tests/contract/test_libraries.py -v --no-header 2>&1 | tail -12
```

Expected: `8 passed`(原 7 + 新 1)。

---

## Task 8: Commit 1 — fixtures + 4 个新测试(subagent review 后再提交)

**Files:**(以下所有都已在前 7 task 落盘)
- `scripts/make_table_sample.py`
- `scripts/make_loop_template.py`
- `tests/fixtures/io_samples/README.md`
- `tests/fixtures/io_samples/table_sample.docx`
- `tests/fixtures/io_samples/loop_template.docx`
- `tests/contract/test_io_smoke.py`(追加 3 个 test)
- `tests/contract/test_libraries.py`(追加 1 个 test)

- [ ] **Step 1: 全量 lint + 类型检查**

```bash
conda run -n scrivai ruff check scripts/ tests/contract/test_io_smoke.py tests/contract/test_libraries.py
conda run -n scrivai ruff format --check scripts/ tests/contract/test_io_smoke.py tests/contract/test_libraries.py
```

Expected: 两条命令都 clean(若 `ruff format --check` 报 would reformat,先跑 `ruff format scripts/ tests/contract/test_io_smoke.py tests/contract/test_libraries.py` 再重新 check)。

- [ ] **Step 2: 跑全量非 smoke 测试确认无回归**

```bash
conda run -n scrivai pytest tests/ --ignore=tests/smoke -q 2>&1 | tail -5
```

Expected: `181 passed, 4 skipped`(原 177 + 新 4)。若失败,回看具体失败 test。

- [ ] **Step 3: 开 code-reviewer subagent review**

调用 `Agent` 工具,subagent_type=`superpowers:code-reviewer`,prompt 模板(替换 `<DIFF>` 为 `git diff --cached` 输出):

```
Review M1.5b commit 1: fixtures + T1.8 edge DoD coverage.

Context:
- Plan: docs/superpowers/plans/2026-04-17-scrivai-m1.5b.md
- Spec: docs/superpowers/specs/2026-04-17-scrivai-m1.5-design.md §8.2
- TD.md T1.7/T1.8 DoD reference

Files added/modified in this commit:
- scripts/make_table_sample.py (NEW)
- scripts/make_loop_template.py (NEW, XML 后处理构造 docxtpl 循环模板)
- tests/fixtures/io_samples/README.md (NEW)
- tests/fixtures/io_samples/table_sample.docx (NEW binary)
- tests/fixtures/io_samples/loop_template.docx (NEW binary)
- tests/contract/test_io_smoke.py (MODIFY: +3 tests: test_docx_to_markdown_preserves_table, test_docx_renderer_list_placeholders_in_loop, test_docx_renderer_render_loop)
- tests/contract/test_libraries.py (MODIFY: +1 test: test_search_with_metadata_filter)

Review focus:
1. Test quality: 断言是否覆盖 DoD 核心("表格结构保留"/"循环渲染"/"metadata filter 生效"),是否有易失败的脆弱断言(如固定 markdown 字节)?
2. Fixture 生成脚本:路径计算用 `Path(__file__).resolve().parents[1]` 是否正确指向 repo root?XML 后处理对不存在占位符抛错是否合理?
3. Plan 对齐:覆盖 TD.md T1.8 的 "docx_to_markdown 处理表格边缘情况" / "DocxRenderer 支持循环"?未覆盖的("公式 / 图片 alt / PDF 表格 OCR")是否可接受本段留白?
4. 生产代码零变更:`scrivai/{knowledge,io}/*.py` 确实未动?
5. Ruff / mypy clean?

Report: APPROVE / CHANGE_REQUESTED(列 blocker 和 minor 建议)。

<DIFF>
[paste git diff --cached output here]
</DIFF>
```

期望 subagent 结果:`APPROVE`。若 `CHANGE_REQUESTED`,按 blocker 修改,重新 review(最多 2 轮;第 3 轮仍不过则通知用户)。

- [ ] **Step 4: stage + commit**

```bash
git add scripts/make_table_sample.py scripts/make_loop_template.py \
        tests/fixtures/io_samples/README.md \
        tests/fixtures/io_samples/table_sample.docx \
        tests/fixtures/io_samples/loop_template.docx \
        tests/contract/test_io_smoke.py \
        tests/contract/test_libraries.py
git status
git commit -m "test: add fixtures + coverage for T1.8 edge DoD (M1.5b)

- tests/fixtures/io_samples/{table_sample,loop_template}.docx
  (by scripts/make_{table_sample,loop_template}.py one-shot builds)
- test_docx_to_markdown_preserves_table:pandoc 保留表格结构
- test_docx_renderer_render_loop + list_placeholders_in_loop:
  docxtpl {% for %} 循环支持
- test_search_with_metadata_filter:qmd hybrid_search filters 透传

生产代码零变更;M0.75 已落地 Library/IO 主体实现,M1.5b 只补 T1.8 边缘 DoD。"
```

Expected: 提交成功,`git log --oneline -1` 显示新 commit。

---

## Task 9: Commit 2 — design.md §4.8 对齐 + TD.md T1.7/T1.8 注解(+ 最终 review + finishing)

**Files:**
- Modify: `docs/design.md:1077` 单行 + 模板约束段后追加一段
- Modify: `docs/TD.md` T1.7(~L597 末尾) + T1.8(~L608 末尾) 各追加一行「实施偏离」

- [ ] **Step 1: 修 `docs/design.md` 第 1077 行**

老:
```python
md: str = pdf_to_markdown("scan.pdf", ocr=True)     # docling
```

改为:
```python
md: str = pdf_to_markdown("scan.pdf")               # MonkeyOCR HTTP(base_url 可覆盖)
```

- [ ] **Step 2: 在 `docs/design.md` §4.8 的 `DocxRenderer 模板制作约束` 段之前(第 1083 行空行后)追加一段 OCR 说明**

插入位置:在代码块结束(第 1082 行 "` ``` `")之后、"**DocxRenderer 模板制作约束**" 之前,插入:

```markdown
**`pdf_to_markdown` 的 OCR 路径**(M0.75 实施偏离,M1.5b 同步):不走 docling 而走 **MonkeyOCR HTTP 服务**(默认 `base_url="http://100.81.95.44:7861"`,由调用方按需覆盖)。流程:POST `/parse` 上传 PDF → 取 `download_url` → GET ZIP → 从 ZIP 抽 `.md` 文件内容。服务不可达时抛 `IOError`,不静默回退。与设计文档最初设想的 docling 本地依赖不同,采用 HTTP 服务的原因:(1) 复用现网已部署的 MonkeyOCR,避免本地 docling 模型加载体积;(2) 业务方可指向自建 OCR。接口签名保持 `pdf_to_markdown(path, *, base_url=..., timeout=...) -> str`,**不含 `ocr=True` 参数**(始终走 OCR)。

```

- [ ] **Step 3: 修 `docs/TD.md` T1.7(在 L597 `- **估时**:1d` 之后追加)**

老(L596-597):
```markdown
- **依赖**:T0.11
- **估时**:1d
```

在 `- **估时**:1d` 之后直接加一行:
```markdown
- **实施偏离**(M0.75+M1.5b,2026-04-17):主体在 M0.75 已落地 — `build_libraries(qmd_client)` 真接 `qmd.connect` 的 `SqliteQmdClient`;`add/get/list/delete` 透传 qmd document-level API;`search` 透传 `hybrid_search`(含 filters);契约测试 `test_libraries.py` 用真 SQLite DB(tmp_path 级)。M1.5b 追加 `test_search_with_metadata_filter` 覆盖 metadata filter 行为。详见 `docs/superpowers/plans/2026-04-17-scrivai-m1.5b.md`。
```

- [ ] **Step 4: 修 `docs/TD.md` T1.8(在 L608 `- **估时**:2d` 之后追加)**

老(L607-608):
```markdown
- **依赖**:T0.12
- **估时**:2d
```

在 `- **估时**:2d` 之后直接加一行:
```markdown
- **实施偏离**(M0.75+M1.5b,2026-04-17):主体在 M0.75 已落地 — `docx_to_markdown` 走 pandoc 子进程、`doc_to_markdown` 走 LibreOffice headless → docx → pandoc、`pdf_to_markdown` 走 **MonkeyOCR HTTP**(design.md §4.8 已同步,非原计划的 docling)、`DocxRenderer` 真调 docxtpl。M1.5b 补齐 T1.8 边缘 DoD 的三项测试:表格 docx 结构保留(`test_docx_to_markdown_preserves_table`)、docxtpl `{% for %}` 循环模板(`test_docx_renderer_render_loop` + `list_placeholders_in_loop`);fixture 通过 `scripts/make_{table_sample,loop_template}.py` 生成入 git。**未覆盖**:公式、图片 alt、PDF 表格扫描件 OCR(留待按需补)。详见 `docs/superpowers/plans/2026-04-17-scrivai-m1.5b.md`。
```

- [ ] **Step 5: 开 code-reviewer subagent review commit 2**

调用 `Agent`,subagent_type=`superpowers:code-reviewer`,prompt:

```
Review M1.5b commit 2: docs alignment.

Context:
- Plan: docs/superpowers/plans/2026-04-17-scrivai-m1.5b.md
- Spec: docs/superpowers/specs/2026-04-17-scrivai-m1.5-design.md §8.2
- Prior M1.5a pattern:docs: align design.md §4.4 with runtime_context mode(commit 33e97dc)

Files modified:
- docs/design.md §4.8:pdf_to_markdown 示例参数从 `ocr=True`(docling 注释)改为默认签名(MonkeyOCR 注释);代码块后新增一段 OCR 路径说明(~250 字)
- docs/TD.md T1.7 + T1.8:末尾各追加「实施偏离(M0.75+M1.5b)」行

Review focus:
1. design.md §4.8 修订是否自洽?是否残留 "docling" 字样(应全删)?
2. TD.md 注解与 M1.5a 已有 T1.4/T1.5/T1.6 注解格式是否一致(见 commit b952c6f)?
3. 是否遗漏 design.md 其他位置 "docling" 引用?`git grep -n docling docs/ scrivai/` 应为空或仅历史 reference。
4. 是否有信息错误(如 base_url 拼写、测试函数名对不上 commit 1)?

Report: APPROVE / CHANGE_REQUESTED。

<DIFF>
[paste git diff --cached output here]
</DIFF>
```

- [ ] **Step 6: stage + commit**

```bash
git add docs/design.md docs/TD.md
git diff --cached --stat
git commit -m "docs: align design.md §4.8 to MonkeyOCR + annotate TD.md T1.7/T1.8 (M1.5b)

- design.md §4.8:pdf_to_markdown 示例改默认签名 + 新增 MonkeyOCR HTTP 路径说明
- TD.md T1.7/T1.8:追加「实施偏离(M0.75+M1.5b)」注解,明确主体在 M0.75 落地,
  M1.5b 只补 T1.8 边缘 DoD 三项测试

同 M1.5a §4.4 对齐模式。docling 引用全删。"
```

- [ ] **Step 7: 最终 sanity check**

```bash
conda run -n scrivai ruff check .
conda run -n scrivai ruff format --check .
conda run -n scrivai pytest tests/ --ignore=tests/smoke -q 2>&1 | tail -5
git grep -n "docling" docs/ scrivai/ 2>&1 || echo "no docling refs"
git log --oneline main..HEAD
```

Expected:
- ruff clean
- `181 passed, 4 skipped`
- docling grep 空(或只残留在 Reference/ 外部目录,可接受)
- 2 commits ahead of main

- [ ] **Step 8: 启动 `superpowers:finishing-a-development-branch`**

调用 `Skill` 工具,skill=`superpowers:finishing-a-development-branch`,args 含当前状态摘要(2 commits、基线 181 passed、M1.5c 依赖 M1.5b 合 main)。

按 skill 流程呈现 4 选项(Merge locally / Create PR / Keep as-is / Discard),用户选择后执行。

**预期用户选 Merge locally**(与 M1.5a 模式一致,M1.5c 依赖)。执行:

```bash
git checkout main
git pull  # 可能失败(远端早于本地 44 commits),若失败跳过
git merge feat/scrivai-m1.5b-library-io --ff-only
conda run -n scrivai pytest tests/ --ignore=tests/smoke -q 2>&1 | tail -5
git branch -d feat/scrivai-m1.5b-library-io
git log --oneline -5
```

Expected: fast-forward 成功,基线 181 passed,分支删除,新 HEAD 含 commit 2 的 hash。

---

## DoD 总对账(M1.5b 验收)

- ✅ `pandoc --version` ≥ 2.5 在 conda env 可用
- ✅ `tests/fixtures/io_samples/{README.md, table_sample.docx, loop_template.docx}` 存在且入 git
- ✅ `scripts/make_{table_sample,loop_template}.py` 可独立重新生成 fixture
- ✅ `pytest tests/` 全绿:`181 passed, 4 skipped`(M1.5a 基线 177 + 新 4)
- ✅ ruff / ruff format clean
- ✅ `scrivai/knowledge/*.py` 和 `scrivai/io/*.py` 内容与 main(pre-M1.5b) 完全一致
- ✅ `git grep "docling" docs/` 空
- ✅ design.md §4.8 pdf_to_markdown 示例与实现签名一致
- ✅ TD.md T1.7/T1.8 含「实施偏离(M0.75+M1.5b)」注解,指向此 plan
- ✅ M1.5b 合 main(用户选 Merge locally)
- ✅ M1.5c 前置解锁(Library + IO 真实现 + T1.8 边缘 DoD 全验证)

---

## Self-Review 结果

**1. Spec coverage(M1.5 spec §8.2 对照)**:
- ✅ `scrivai/knowledge/base.py` MODIFY:M0.75 已做,M1.5b 文档注解覆盖
- ✅ `scrivai/knowledge/factory.py` MODIFY:M0.75 已做
- ✅ `scrivai/io/convert.py` MODIFY:M0.75 已做,M1.5b design.md 对齐 MonkeyOCR
- ✅ `scrivai/io/render.py` MODIFY:M0.75 已做
- ✅ `test_libraries.py` MODIFY:Task 7 追加 metadata filter
- ✅ `test_io_smoke.py` MODIFY:Task 5/6 追加 3 个
- ✅ `tests/fixtures/io_samples/` NEW:Task 2-4
- ❌ `sample.pdf` fixture:未做(MonkeyOCR smoke 服务不可达时跳过,硬 fixture 无处验证);TD.md T1.8 注解明示"未覆盖"为可接受留白

**2. Placeholder scan**: 零 TBD / TODO / 省略号 / "fill in"。全部代码 / 命令 / 预期输出均给出。

**3. Type consistency**:
- `DocxRenderer.render(context, output_path) -> Path` 签名在 Task 6 与 M0.75 现有实现 `scrivai/io/render.py:52` 一致 ✓
- `_BaseLibrary.search(query, top_k, filters) -> list[SearchResult]` 在 Task 7 与 `scrivai/knowledge/base.py:62-69` 一致 ✓
- `docx_to_markdown(path) -> str` 在 Task 5 与 `scrivai/io/convert.py:23` 一致 ✓

**4. Commit 粒度**:2 个 commit 与计划一致(tests + docs)。code-reviewer 在每个 commit 前跑。

---

## Unknowns / 风险备忘

- **loop_template.docx 生成的 Step 3 验证失败概率**:低 — docxtpl 对 XML 后处理生成的模板支持良好,但若 python-docx 版本升级导致 document.xml 结构变化,可能占位字符串拆分。回退:改用 `{%p ... %}` 段落级循环(docxtpl 自带 run-join 预处理)。
- **pandoc 2.5 表格默认输出格式差异**:2.5 默认走 "pipe_tables",但 CI 若换 3.x 可能改为 "grid_tables"。Task 5 已用 `"|" in md or "+-" in md` 兼容两种。
- **fixture 二进制 docx 入 git**:合计 ~70 KB,可接受(Reference/ 目录已入若干 MB 外部 PDF)。
- **若 MonkeyOCR 服务长期不可达**:`test_pdf_to_markdown_smoke` 将长期 skip;不影响 M1.5b DoD。M2 或 M3 可考虑加本地 PDF→MD 回退路径。
